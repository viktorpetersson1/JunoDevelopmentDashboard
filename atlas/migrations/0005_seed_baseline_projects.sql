-- Applied via mcp__supabase__apply_migration as `atlas_seed_baseline_projects`.
-- Generated by atlas/scripts/generate-seed-sql.mjs from public/data.js
-- BASELINE_PROJECTS. Money in cents, percentages in bps. Idempotent.
--
-- Regenerate: cd atlas && node scripts/generate-seed-sql.mjs
-- Apply: copy file contents into a `mcp__supabase__apply_migration` call.

-- (10 INSERT statements; one per BASELINE_PROJECTS entry p2..p11.)
-- Full SQL omitted from this committed file because it would be ~10 KB of
-- duplicated boilerplate; the canonical source is the generator script.
-- The script writes scripts/seed-baseline-projects.sql on every run.
