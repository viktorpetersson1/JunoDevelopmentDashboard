'use client';

/**
 * Actuals CSV importer modal -- stub (V6.1 T108).
 * Full implementation lands in T108. This stub keeps actuals-client.tsx compiling.
 */

export function ActualsImporter({
  projectKey: _projectKey,
  projectUuid: _projectUuid,
  onClose: _onClose,
}: {
  projectKey: string;
  projectUuid: string;
  onClose: () => void;
}) {
  return null;
}
