# Atlas V6.1 — Editable Platform + Home/Projects/Pipeline UX + Ask Juno Agent · Build Tracker

> **Handle:** `V6.1` · **Full name:** _Atlas V6.1 — Editable Platform + Home/Projects/Pipeline UX Rebuild + Ask Juno Agent_ · **Ships as tag:** `v6.1.0` (Part 1 milestone: `v6.1.0-beta.1`)
>
> **This is the SOLE active Atlas workstream until `v6.1.0` is tagged.** No new feature work outside V6.1.
>
> - **Planned (source of truth):** [`CLAUDE_CODE_INSTRUCTIONS_V6_1.md`](CLAUDE_CODE_INSTRUCTIONS_V6_1.md) — verbatim from Viktor's 2 Jun 2026 docx, staged at `a609be4`.
> - **Actual:** the Status + Commit columns below — updated as each chunk lands on `main`.
> - **Supersedes:** V5.2 (tagged `v5.2.0`, shipped 2 Jun 2026 — see [`V5_2_TRACKER.md`](V5_2_TRACKER.md)).
> - **Workflow (Viktor, carried from V5.2):** direct commits to `main`, push each, auto-deploy. **No per-ticket PRs except the §0 ACK** (folded into main per "just go").
> - **Blocked values:** scaffold-build, leave un-seeded behind a `BLOCKED-ON-VIKTOR` marker; nothing ships to prod with invented numbers.
> - **Hard Rules:** (1) no removed Excel inputs · (2) no calc changes without passing `pnpm test:golden` · (3) no new UI libs (compose `ja-*`) · (4) no stage transition without approval snapshot · (5) **NEW** no write API without role check + audit log + re-approval gate. Engine UNTOUCHED. Migrations **0029–0033 only**; 0000–0028 frozen.
>
> **Status legend:** ☐ NOT STARTED · ◐ IN PROGRESS · ✅ DONE · ⛔ BLOCKED · ⤵ DEFERRED · ⊘ FOLDED · ⚠️ DRIFT-NOTED (see register below)

---

## Ticket status (planned → actual)

| Ticket   | Scope (planned)                                                                      | Pri | Mig        | Status | Commit(s)       | Notes / drift                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------- | ------------------------------------------------------------------------------------ | --- | ---------- | ------ | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **§0**   | ACK (`chore: ACK CLAUDE_CODE_INSTRUCTIONS_V6_1`) — `ACK_V6_1.md`                     | P0  | —          | ✅     | `9db77bd`       | ACK folded into `main` (`9db77bd`) per Viktor "just go" — artifact on main; PR ceremony skipped (V5.2 precedent). gh was unauthed so no GitHub PR created.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **T104** | Inputs editor modal + `PATCH /api/projects/[id]` + audit + calc re-run + re-approval | P0  | 0029, 0030 | ✅     | _(this commit)_ | **SHIPPED.** PATCH (E1-gated) + editor modal + audit(`source`) + re-approval drift chip. Mig 0029/0030 applied. Engine untouched; golden 23/23, 426 tests; typecheck + lint clean. Resolves DR-3/DR-4. Deviations V6-01a–g (see DEVIATION_REGISTER).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **T105** | Monthly P&L on Summary + dynamic chart timeline platform-wide                        | P0  | —          | ✅     | `baa8015`       | **SHIPPED.** `lib/charts/project-window.ts` (projectWindow/portfolioWindow/autoWindow), `buildProjectPnLMonthly` + golden test (11 tests), `monthly-pnl-table.tsx` client component (sticky labels/totals, "Show full model horizon" toggle). CashFlow + PortfolioCashFlow trimmed by default. 444/444 tests, typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **T106** | Assumptions hero block on Summary + cash-flow debt-outstanding overlay               | P0  | —          | ✅     | `c92301a`       | **SHIPPED.** `AssumptionsHero` (3×4 KPI grid above 9-line P&L; every cell from `ProjectInput`/`ProjectKpis`). `debt_outstanding` added to `CashFlowMonth` + monochrome dashed Line overlay on cash-flow chart. Reconciliation test asserts overlay == `debtSnapshotForMonth` (same engine source: `debt_balance`). Engine untouched. 445/445 tests; typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **T107** | Structured cost breakdown editor (line items) + JSONB schema                         | P0  | 0031       | ✅     | `c5f033e`       | **SHIPPED.** Mig 0031 (additive `cost_breakdown jsonb`) applied via MCP. Zod `CostBreakdownSchema` (4 groups × line items: label/amount/note/status). Inputs editor gains 7th `<Section>` with collapsible groups, inline add/delete, status select, and divergence StatusDot when itemized total vs lump-sum mirror diverges > 5%. Engine reads only lump-sum fields (Hard Rule #2). Deviations V6-05.a (Section vs tab) + V6-05.b (inline `CostBreakdownShape` in types.ts keeps calc module Zod-free). 459/459 tests.                                                                                                                                                                                                                                                                                                       |
| **T108** | Smart CSV import for Actuals — LLM column mapper + dry-run + commit + audit          | P0  | —          | ✅     | `ed27d15`       | **SHIPPED.** Two-phase endpoint (dry_run → commit) with E1 gating + `source='csv_import'` audit. Zero-dep `lib/csv/parse.ts` (RFC-4180-ish). LLM column mapper via raw `fetch()` to Sonnet 4.5 with fallback chain (no SDK — Hard Rule #3). Per-row category normalization with synonym table; unknown categories mark row invalid (never fabricates). Stateless: rounds-trips validated rows in response; server re-validates on commit. Bulk insert in single supabase `.insert([...])` (Postgres aborts batch on any constraint failure = spirit of rollback). 3-step Modal UI (Upload / Preview / Success) with confidence dot + mapping table + per-row badges. Deviation V6-06.a (XLSX deferred to V6.1 v2 — no parser; helpful error redirects to "Save As CSV"). 14 new tests (parse 8, mapper 6). 459/459 full suite. |
| **T109** | `POST /api/projects` + Sales/Risks/Timeline/Actuals-row CRUD                         | P0  | 0032       | ✅     | `d009651`       | **SHIPPED.** Mig 0032 (atlas.project_risks) applied. Full Risks CRUD: repo + `GET/POST /api/projects/[id]/risks` + `PATCH/DELETE /api/risks/[id]` + `RisksClient` UI. `SalesEditorModal` (sale price + psf + margin via PATCH). `DELETE /api/actuals/[id]` (soft-archive). stage='tbc' default. Projects list isEditor-gated. Create modal = existing wizard nav (DR-4 confirmed). Timeline date edits deferred — PATCH already covers date fields via existing Inputs editor (E8-8: timeline dates are purchase_date/durations, all in InputsEditor). ⤵ **Actuals row PATCH (edit)** deferred to T108 (LLM import handles bulk; single-row edit is low-priority).                                                                                                                                                            |
| **T110** | Home redesign: Boardroom Strip + Today's Desk + monthly chart + Annual P&L           | P0  | —          | ✅     | `591c397`       | **SHIPPED.** Two-column layout (60% Boardroom Strip / 40% Today's Desk), tactical 3-cell strip, `PortfolioCashFlowChart`, `AnnualPnLTable` promoted from `/analytics/forecast`. Deleted `/analytics/forecast/page.tsx`. 301 redirect added. Legacy Chip/SmallChip/ActionCard removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **T111** | Projects list redesign: Table (default) + Map + Timeline                             | P0  | —          | ✅     | `591c397`       | **SHIPPED.** Sortable 7-column table (Project/Stage/$/sqft/Revenue/Margin/Sale month/Owner), view toggle (Table/Map/Timeline) persisted in localStorage. Timeline = div-based Gantt. **Deviation V6-07.c:** Map view = "coming soon" placeholder; MapLibre GL is NOT in `package.json` despite the plan claiming it's "already in the stack via the comp researcher" — confirmed by grep. Adding it would violate Hard Rule #3.                                                                                                                                                                                                                                                                                                                                                                                                |
| **T112** | Pipeline editability: drag-and-drop + inline candidate edits + velocity goal modal   | P0  | —          | ✅     | `591c397`       | **SHIPPED.** `pipeline-board.tsx → 'use client'` with native HTML5 DnD (no dnd-kit). Drop calls `PATCH /api/projects/[id]`; optimistic update + revert on failure. Editor-gated drag handles. New `PATCH /api/globals/velocity` endpoint + inline edit form in `GoalTracker`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **T113** | `<StatusDot>` pulsating-dot primitive + platform-wide alert-banner sweep             | P0  | —          | ✅     | `71f5829`       | **SHIPPED.** New primitive (6px pulsating dot + hover popover + `prefers-reduced-motion` + `suppressIfZero` + `StatusDotGroup`). Swept 3 offenders: `/pricing` stale banner, KpiTile YoY tags, project re-approval chip. **DR-5 resolved: reused existing COLOR_TOKENS** (#a16207 / #b91c1c / #0D0D0D) rather than the doc's brighter hexes for platform consistency. 433/433 tests.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **T114** | "Finance & Analytics" rename + Activity tab compressed to vertical timeline          | P1  | —          | ✅     | `591c397`       | **SHIPPED.** Sidebar label `Analytics → Finance & Analytics`. Activity tab compressed from 4-col grid to GitHub-style vertical timeline (12px label · 14px body · hairline dividers). AppShell test updated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **🏷️**   | Tag `v6.1.0-beta.1` on T114 merge (end of Part 1)                                    | —   | —          | ✅     | `591c397`       | **TAGGED + PUSHED** — `v6.1.0-beta.1`. End of Part 1 milestone (T104–T114 + T113 shipped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **T115** | Ask Juno agent v1: tool-calling + tool registry + confirmation card + $10K threshold | P0  | —          | ✅     | `d61cfe4`       | **SHIPPED.** `lib/ask-juno/{system-prompt,risk-classifier,tools}.ts` — 8 tools (4 READ auto-exec, 4 WRITE risk-classified). Risk classifier: $10K threshold, batch≥5, locked snapshot, viewer-role guards (20 unit tests). POST `/api/ask-juno` upgraded to Node-runtime tool-calling loop. AskJunoWidget gains confirmation cards + tool status + audit log id display. Per-owner row visibility (B-2 blocker) deferred — agent uses authenticated user's role for write gating; per-owner read-row filtering is V6.3 scope.                                                                                                                                                                                                                                                                                                  |
| **T116** | Ask Juno agent v2: file ingestion (CSV/XLSX/PDF/DOCX) + structured proposal cards    | P0  | —          | ✅     | `d61cfe4`       | **SHIPPED.** POST `/api/ask-juno/ingest` (Node runtime, 60s timeout, E1-gated, editor+). Sonnet 4.5 extracts line items from CSV (via T108's `parseCsv`) or PDF (base64 document block). 📎 file input button in widget; ingest_proposal renders summary with category breakdown + confidence + redirect to Actuals tab for commit. **Deviation V6-09.a:** XLSX rejected with helpful "Save As CSV" redirect (same V6-06.a basis: no XLSX parser). DOCX falls through to text-extraction path — basic but functional.                                                                                                                                                                                                                                                                                                          |
| **T117** | Closing PR: DECISIONS + DEVIATION_REGISTER + acceptance pass + tag `v6.1.0`          | P0  | —          | ✅     | _(this commit)_ | **SHIPPED.** D-044→D-056 logged in DECISIONS.md. V6-01→V6-10 logged in DEVIATION_REGISTER.md. Tracker reflects full status. Tag `v6.1.0` pushed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

Migration allocation: **0029** projects versions extras (T104 ✅) · **0030** audit_log `source` (T104 ✅) · **0031** projects cost_breakdown (T107) · **0032** project_risks (T109) · **0033** spare/unallocated.

---

## Drift register (doc vs. codebase) — verified against repo at `a609be4`, 2 Jun 2026

Same discipline as V5.2's "Corrected assumptions" pass. The doc drifted from repo reality in these places; resolutions below are applied unless Viktor overrides.

- **DR-1 — Decision-ID collision (HIGH).** Plan assigns V6.1 decisions `D-043 → D-055`. But **`D-043` is already taken** by V5.2 ("Platform-wide Ramp-grade visual pass"). **Resolution:** shift V6.1 to **`D-044 → D-056`** (+1). T104→D-044 … T116→D-056. **[Applied: D-044 logged in DECISIONS.md.]**
- **DR-2 — Migration numbering / §9 factual error (MED).** Real migrations on disk: `0000–0025`, `0027`. **`0026` and `0028` never existed.** §9 claims rollout-globals = "migration 0028" — wrong, it's `0025_globals_rollout.sql`. **Resolution:** V6.1 new migrations start at **0029** per the plan (gaps are harmless). **[Applied: 0029 + 0030 created at the real tail.]**
- **DR-3 — Audit table already has before/after (MED).** `atlas.audit_log` already has `before_json` + `after_json`; no `source`/`action`; rows need `org_id`. **Resolution:** migration **0030 adds `source` only**. **[✅ Applied in T104.]**
- **DR-4 — Project-create path already built (MED).** `POST /api/projects` + `/projects/new` wizard + `createProject` + `CreateProjectSchema` already exist and are E1-gated. TRUE plan gaps: no Inputs-edit affordance, no `PATCH` (both T104). **Resolution:** T109 create-scope reduces (stage=`tbc`, return `ProjectResult`, create modal, audit-on-create). **[✅ T104 added the PATCH + editor; T109 handles the rest.]**
- **DR-5 — StatusDot colors diverge from COLOR_TOKENS (MED, T113).** Doc warning `#D4A017` / error `#C0392B` vs `--color-warning #a16207` / `--color-negative #b91c1c`. **Recommendation:** reuse existing tokens for consistency. **Awaiting Viktor at/before T113.** (T104's placeholder re-approval chip already uses the existing `--color-warning`.)
- **DR-6 — §9 "three numbers" status (LOW for Part 1).** KPC LOC ✅, NPAT target ✅ $8M; owner↔auth linkage ◐ 1/7 → affects **T115** only. Not a Part 1 blocker.

### Lower-priority notes (apply at ticket time)

- Two viewer roles (`viewer_basic`, `viewer`) — `requireEditor` allows only `super_admin`/`editor`, so both viewers are read-only. No `admin` role.
- New-table RLS (0032 project_risks) sources role from **`public.user_profiles`** (not `atlas.profiles`).
- API routes key off stable `project_key`; `findLatestLockedSnapshot` takes the project UUID. **T104 keeps that UUID stable across edits** (see V6-01a) so snapshot linkage survives version bumps.
- `gh` CLI installed (2.87.3) but not authenticated; no token in env. PRs need Viktor (or a token) — moot under the direct-to-main workflow.

---

## Blockers

| #   | Blocker                                                   | Severity | Affects                            | Status                                                                                                  |
| --- | --------------------------------------------------------- | -------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------- |
| B-1 | `gh` not authenticated; no token in env                   | Low      | §0 ACK PR creation                 | RESOLVED — ACK folded into `main` via direct merge (`9db77bd`); no further PRs needed (direct-to-main). |
| B-2 | Owner↔Supabase `user_id` linkage for 6 of 7 owners       | Med      | T115 per-owner earnings visibility | OPEN — only Viktor linked; carried from V5.2 D-032/T097.                                                |
| B-3 | StatusDot palette decision (existing tokens vs new hexes) | Low      | T113                               | OPEN — awaiting Viktor; default = reuse existing tokens.                                                |

Resolved before sprint start: KPC LOC numbers (✅), NPAT target $8M (✅) — see DR-6.

---

## Critical dependencies (doc §6.3)

- **T104** (PATCH API) ✅ merged — unblocks T109, T111, T112, T115.
- **T108** (CSV importer) must merge before T116 (reuses its commit path).
- **T113** (StatusDot) before T110 — or land placeholder spans and replace in T113's PR. (T104 already uses a placeholder chip.)
- **T109** (`POST /api/projects`) before T115.
- **Part 2 (T115–T117)** does not start until T104 + T108 + T109 are merged.

## Definition of done (every ticket, doc §6.5)

1. Merged to `main` · 2. CI green · 3. Verified on https://juno-atlas.pages.dev · 4. `DEVIATION_REGISTER.md` updated · 5. `DECISIONS.md` updated where applicable · 6. Audit-log spot-check on new write paths.

### T104 — done-when (doc) status

- [x] Migrations 0029 + 0030 applied; existing data backfilled
- [x] `PATCH /api/projects/[id]` returns the new `ProjectResult` with calc engine output
- [x] Inputs editor modal opens, pre-fills, validates inline, saves atomically
- [x] Audit log row on every save with full before/after JSONB (+ `source='ui'`)
- [x] Locked snapshot → `pending_reapproval` with header placeholder (amber "Re-approval needed" chip)
- [x] `pnpm test:golden` green (engine untouched) — golden 23/23, full suite 426/426
- [x] Vitest unit test for `updateProject` (happy / validation / locked-snapshot gate / engine error / not-found)
- [~] Playwright E2E — skeleton, skipped pending a seeded editor account (matches existing suite)
- [x] DEVIATION_REGISTER.md row added (V6-01)
- [x] DECISIONS.md D-044 logged
