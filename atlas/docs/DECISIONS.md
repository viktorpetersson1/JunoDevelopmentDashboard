# Atlas — Resolved decisions

Source-of-truth log for D-decisions from the handoff bundle's
`DECISIONS.md`. Bundle copy is read-only; this file is the live record.

Format: ID · Topic · Status · Resolution · Notes

---

## Pre-build (Week 0)

| ID    | Topic           | Status | Resolution                                                                                                            |
| ----- | --------------- | ------ | --------------------------------------------------------------------------------------------------------------------- |
| D-001 | Hosting         | ✅     | **Supabase + Render** (not Vercel + Neon). Existing infra reuse. See `SUPABASE_TRANSLATION.md`.                       |
| D-002 | Auth            | ✅     | **Supabase Auth** (email + password). Replaces the bundle's Clerk recommendation.                                     |
| D-003 | Domain          | ✅     | Live at https://juno-dashboard.onrender.com. Custom domain TBD.                                                       |
| D-004 | Data residency  | ✅     | **US-only** (Supabase US region). No EU/UAE constraint surfaced.                                                      |
| D-005 | Excel canonical | ✅     | **Decommissioned.** `public/engine.js` is the canonical source; vanilla snapshot drives golden tests (T032' + T034'). |

---

## Post-build (resolved 25 May 2026)

### D-006 — P0 deadline (17 June 2026)

**Status:** ✅ On track / effectively moot
**Resolution:** Today is 25 May 2026. 27 of 41 P0 tasks shipped (everything
except the deliberately deferred T025-T030 modular calc split, which is
internal refactor and doesn't gate ship). 17 June target will be hit.

### D-007 — Pricing data source (W1.7 comps)

**Status:** ✅ Resolved 31 May 2026
**Resolution:** Functionally closed by the D-024 → D-026(b) pricing-engine
sprint. AI comp research (`lib/pricing/comp-researcher.ts`) is now the
primary data source — live web search across Zillow / Realtor.com /
Out East / Compass / Douglas Elliman / Bespoke / Sotheby's, with a
training-data fallback. As of 31 May, the `anthropic-beta` regression
that was silently dropping every call to knowledge-only is fixed (D-026b),
so the path is genuinely live. The original "manual + CSV" recommendation
remains available via `/pricing/comps/new` and the CSV importer at
`/pricing/comps/import` — useful for human-curated anchor comps from
private MLS data Viktor brings in — but is no longer the canonical path.
Library at this point: 217 comps (mostly AI-estimated 2024 vintage from
the pre-fix era; live MLS recency takes effect on the next research run).

### D-008 — Owner email convention + master admin

**Status:** ✅ Provisioned
**Resolution:** `viktor.petersson@kpconfidencia.com` is the master admin.
Verified existing in Supabase:

- `auth.users.id` = `78d5d1e8-0902-435f-b367-a3d706a1ff00`
- `public.user_profiles.role` = `super_admin`
- `public.user_profiles.display_name` = `Viktor`

Additional owner accounts (Peter, Lars, Philip, Missy, Massi, Mark) to
be provisioned ad-hoc as they need access. Convention: corporate emails
preferred (`firstname@kpconfidencia.com`) but personal emails accepted
if a corporate alias isn't available.

### D-009 — Currency display

**Status:** ✅ Approved
**Resolution:** **USD only** for year 1. No EUR/AED display anywhere.
KPS construction-cost feed (P2 W2.3) will convert to USD at the data
layer via an `fx_rate` table updated monthly.

### D-010 — Backup & recovery

**Status:** ⏸️ Deferred
**Resolution:** Leave Supabase default backups for now. Revisit after
the dashboard has been in daily use for a quarter and we know what RTO
matters in practice.

### D-011 — Visibility tiers

**Status:** ✅ Approved as recommended
**Resolution:** Three-tier model:

1. All owners see portfolio KPIs + project summaries + project pricing +
   project risks + project drift.
2. Owners see their own capital-call amounts and history. They do NOT
   see other owners' amounts by default.
3. Admins (Viktor + Peter) see everything including per-owner capital
   calls.

Per-project visibility flag ("only Peter + Viktor until Committed")
ships in W1.6.

### D-012 — Recurring cadence

**Status:** ℹ️ FYI / noted
**Resolution:** Atlas won't replace the weekly call, but input from
those calls may change our roadmap. Treat Atlas as augmenting the
existing weekly Peter+Viktor cadence; converge organically over the
next 6-12 months as Operating Cadence (W1.2) earns its place.

---

## Open D-decisions (added post-bundle)

These are real design questions that came up during the build but
aren't in the original bundle. Keep here so they don't get lost.

| ID    | Topic                                       | Status                                                                | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | ------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-013 | Waterfall + LP analysis port                | ✅ Resolved 1 Jun 2026 (T092)                                         | All 4 deferred `aggregatePortfolio` outputs now match vanilla + are golden-tested: `kpis.contingency` + `kpis.sales_metrics` (engine.js:567-599) and `waterfall` + `hypothetical_lp` (engine.js:511-512, via `lib/calc/waterfall/compute.ts`). Investors read from `globals.investors` exactly like vanilla — atlas prod globals carries none (cap table lives in atlas.owners/cap_table), so the aggregator's `waterfall` is `[]` in prod and the `/waterfall` page keeps computing its own from the live cap table; the golden fixture carries investors in globals, so the test exercises the full per-investor 5-tier math (7 investors × 21 numeric fields + 17 tier fields, within 0.5%/$1). Golden 23/23; full suite 373/373. Known-degenerate coverage (non-blocking follow-up): baseline is all-pro-rata (pref/hurdle/carry=0, no promote) + `hypothetical_lp` null (share=0) — real tier3a/tier4 + a non-null LP path need a synthetic-data fixture variant. NB: a separate D-013 row above (canonical post-login surface) is a pre-existing ID collision, not introduced here. |
| D-014 | `juno13` fiscal-year mode                   | ✅ Resolved 2026-05-28                                                | Decommissioned. `juno13` was a legacy convention that rolled Jan-2030 into FY29 (13-month FY29). Fiscal years are now calendar-aligned (Jan-Dec → FYyy) and the annual rollup auto-extends to cover whatever years projects span. Removed: `Globals.fiscal_year_mode` type field, `BASELINE_GLOBALS.fiscal_year_mode`, the Settings → General dropdown, `/api/globals` PATCH field, `atlas.globals.fiscal_year_mode` column (migration `0010_drop_fiscal_year_mode.sql`). `fyOf()` in `lib/calc/portfolio/aggregate.ts` is now pure calendar. Vanilla portfolio snapshot's `annual` block updated — Jan-2030 contributions split out of FY29 into the new FY30. All 351 tests pass.                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-023 | OPEX + tax fields in typed Globals          | ✅ Resolved 2026-05-28                                                | V4.11d slice. Promoted `annual_opex_usd`, `opex_growth_rate`, `apply_tax`, `tax_rate_pct`, `tax_state_rate_pct`, `loss_carryforward` from the local `ExtendedGlobals` shim in `aggregate.ts` to the typed `Globals` interface. Backed by 6 nullable columns on `atlas.globals` (migration `0012_opex_tax_columns.sql`) + Settings → General editor sections OPEX + Tax. `BASELINE_GLOBALS` defaults now match vanilla (USD 475k OPEX / 0% growth / 21% federal + 4.5% state / NOL on) — closes a parity gap where atlas was effectively running at 0% tax. The remaining `ExtendedGlobals` shim only carries `kpc_loc` (nested object, separate ticket). Baselines drift test extended to cover the 6 new fields.                                                                                                                                                                                                                                                                                                                                                                         |
| D-015 | Risk thresholds in typed Globals            | ✅ Resolved 2026-05-28 (org-wide only; per-project override deferred) | The five real tunables that drive `lib/risk/portfolio-risk.ts` (`safeLtcPct`, `salesDelayGraceMonths`, `costOverrunRatio`, `equityClusterPctile`, `saleDownsideHaircut`) are now typed Globals (`risk_*`). New `thresholdsFromGlobals()` helper extracts them with fallback to `DEFAULT_RISK_THRESHOLDS`. `/risks` page reads active globals and passes through. New "Risk thresholds" section in Settings → General editor. Backed by 5 nullable numeric columns on `atlas.globals` (migration `0011_risk_threshold_columns.sql`). Note: the legacy `risk_peak_equity_threshold` / `risk_max_debt_threshold` / `risk_min_moic` / `risk_min_irr_annual` / `risk_min_margin_pct` fields that appeared in vanilla snapshots are NOT used by the atlas risk engine — they were a red herring. Per-project override remains deferred until "risk tuning gets serious."                                                                                                                                                                                                                        |
| D-016 | Exit Pricing Framework (new module)         | ✅ Shipped 26 May 2026                                                | Full v1 build shipped in 7 commits (`682dae2..78c476c`). End-to-end loop: comp library → draft run → engine pre-fills L/B/H from strongest anchor → human edits + commits → engine classifies (rider/stretch_rider/maker) + scores confidence (high/medium/low) + flags data gaps → apply pushes to calc engine. All 8 open questions resolved (see [backlog/exit-pricing-open-questions.md](backlog/exit-pricing-open-questions.md) + [backlog/exit-pricing-proposal-v1.md](backlog/exit-pricing-proposal-v1.md)). 31 new tests (16 worked-example regression + 10 schema invariant + 10 engine helper + 4 multi-plot revenue path + 1 hygiene). Surface 26 (`/pricing`) + Surface 27 (project Pricing tab) live in the sidebar nav.                                                                                                                                                                                                                                                                                                                                                     |
| D-017 | Hosting move: Render → Cloudflare Pages     | ✅ Shipped 25 May 2026                                                | Atlas now lives at https://juno-atlas.pages.dev (CF Pages account `2aae10d326007e9dc5db9c5741abe1fb`, project `juno-atlas`, auto-deploy on push to `main`). Vanilla SPA stays on Render. Pages Functions runtime requires `nodejs_compat` flag set in the CF dashboard (wrangler.toml doesn't apply to Pages). Atlas schema must be exposed via PostgREST (`ALTER ROLE authenticator SET pgrst.db_schemas TO 'public, atlas, graphql_public'`). Both fixes documented in `atlas/docs/deploy-cloudflare.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-018 | Preflight gate before CF build              | ✅ Shipped 25 May 2026                                                | `scripts/preflight.mjs` runs 7 checks before `pnpm run pages:build` to catch deploy footguns in <10s (vs CF's 90s build cycle). Checks: env presence, Supabase URL format, key shape (JWT or sb\_\* handles), edge-runtime exports on every server route, atlas schema PostgREST exposure (remote), CF nodejs_compat flag (remote), Sentry DSN (warn). Wired as a pre-step on `pages:build`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-019 | Excel parity policy: smoke not golden       | ✅ Shipped 26 May 2026                                                | `tests/smoke/excel-parity.test.ts` surfaces drift between the TS calc engine and the original Excel model on the 10 baseline projects. Reports the full delta table for visibility; only FAILS on >30% per-project drift (catches decimal-shift bugs, not normal engine evolution). Current state: 8/10 projects exact match, P2 +10%, P4 +5%. Hard parity (byte-equivalent) is enforced separately by the 19 vanilla golden tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-020 | Sentry on by default, silent when DSN unset | ✅ Shipped 26 May 2026                                                | `@sentry/nextjs` wired into all 3 runtimes (client / edge / server) via configs that `if (dsn) Sentry.init(...)` — local dev + CI without a Sentry project stay silent. `tracesSampleRate=0` (errors only). `beforeSend` strips Authorization/Cookie/x-supabase-auth headers defense-in-depth. Production opt-in by setting `SENTRY_DSN` + `NEXT_PUBLIC_SENTRY_DSN` in CF env vars.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-021 | Owner invites + CF API token rotation       | ⏸️ Deferred indefinitely                                              | Per Viktor 26 May 2026 — too early to invite the 6 owners (Peter, Lars, Philip, Missy, Massi, Mark); revisit at UAT. CF API token `cfat_LMcf…` (shared in chat for diagnostic purposes) stays on the security debt list; Atlas runtime does not use it (it was MCP-only) so impact is bounded to whoever might use it to redeploy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-022 | CI perf gates (Lighthouse + calc engine)    | ✅ Shipped 26 May 2026                                                | `lighthouse` CI job runs against `/sign-in` (only unauth page) with Lighthouse v12 budgets: performance ≥80 / a11y ≥90 (error), best-practices ≥85 / SEO ≥80 (warn). Calc engine latency budget in `lib/calc/__tests__/perf.test.ts`: runProject p99 <5ms, aggregatePortfolio(10) p99 <50ms (currently ~0.22ms and ~1.3ms respectively — 10-100x headroom). Catches order-of-magnitude regressions before they hit CF Pages Functions wall-clock budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-013 | Canonical post-login surface                | ✅ `/dashboard` (shipped 26 May 2026)                                 | Per V3 sprint §T081.3. Was: root `/` served the Overview AND was the post-login bounce target — `/sign-in?redirectTo=/` looped. Now: Overview moved to `app/dashboard/page.tsx`; `app/page.tsx` is a thin server redirect to `/dashboard`; middleware coerces unauthenticated `/` to `redirectTo=/dashboard`; sign-in form defaults `redirectTo` to `/dashboard`; safe-redirect allowlist (D-014 sibling) defaults to `/dashboard`. Sidebar Overview href updated; AppShell test asserts the new href. Alternative (b) — keep Overview at `/` — was rejected because every example in the V3 doc assumed `/dashboard`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-014 | CSP policy + known relaxations              | ✅ Shipped 26 May 2026; tighten via nonce in a follow-up              | Per V3 sprint §T082.2. Each enumerated HTML route in `public/_headers` carries: `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' https://*.supabase.co wss://*.supabase.co; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`. `unsafe-inline` + `unsafe-eval` on `script-src` are temporary — Next.js's inlined client bundles + runtime preamble need them today. Plan: adopt Next.js nonces (`experimental.cspNonce` or custom middleware) in a follow-up sprint and drop both. `frame-ancestors 'none'` defends against clickjacking even before HSTS / X-Frame-Options sniffs in. The HTML-route block is duplicated per route rather than inherited from a `/*` catch-all (Cloudflare Pages concatenates header values from multiple matching rules — see D-019 / T082.1 for the gory details).                                                                                                                                                         |

---

## V4 sprint (28 May 2026)

| ID                  | Topic                                            | Status                                                       | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------- | ------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-014 (V4.10)       | Fiscal-year mode — drop juno13                   | ✅ Shipped 28 May 2026                                       | `fyOf()` simplified to pure calendar-year labels (`FY${yy}`). No toggle UI, no juno13 special-case. Vanilla snapshots regenerated; `portfolio.json` annual block gains FY30. Migration `0010_drop_fiscal_year_mode.sql` applied.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-015 (V4.11a)      | Risk threshold fields → typed Globals            | ✅ Shipped 28 May 2026                                       | `risk_safe_ltc_pct`, `risk_sales_delay_grace_months`, `risk_cost_overrun_ratio`, `risk_equity_cluster_pctile`, `risk_sale_downside_haircut` promoted to `Globals`. `thresholdsFromGlobals()` helper wires risk engine. Settings → General gains Risk thresholds section. Migration `0011_risk_threshold_columns.sql` applied.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-023 (V4.11d)      | OPEX + tax fields → typed Globals                | ✅ Shipped 28 May 2026                                       | `annual_opex_usd`, `opex_growth_rate`, `apply_tax`, `tax_rate_pct`, `tax_state_rate_pct`, `loss_carryforward` promoted. Closes 0% effective-tax parity bug vs vanilla (21%+4.5% now default). Migration `0012_opex_tax_columns.sql` applied. `ExtendedGlobals` shim shrinks to `kpc_loc` only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| D-024 (V4.12)       | Pricing Framework v2 — AI comp research          | ✅ Shipped 28 May 2026                                       | `/pricing/new` quick-price flow: paste Google Maps URL → parse + geocode (Nominatim) → AI comp research (Anthropic w/ `web_search` beta, fallback to knowledge) → 5-stage inline analysis (cost stack / comp evidence / exit corridor / margin model / 20-60-20 probability weighting). No DB writes on research path; "Save comps to library" batch-posts to `/api/comps`. D-007 comp-data-source question partially resolved: MLS data via AI research in production; manual/CSV still canonical for committed runs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-025a (V4.13)      | Pricing Strategy Brief — top-down recommendation | ✅ Shipped 28 May 2026                                       | Replaces the bottoms-up L/B/H pricing-runs UX with a single-screen Strategy Brief generated by Claude (mirrors the Perplexity v5.3 IC report format Viktor already produces manually). 10 sections: recommendation / breakeven thresholds / quick math / comp evidence / market sentiment / reduction ladder / outcome scenarios / risks / why this number / IC framing. New tables: `atlas.pricing_briefs` (versioned per project, partial unique on status='applied'). New Globals: `closing_cost_variable_pct` (0.049 default) + `closing_cost_fixed_usd` (24500 default). API: `POST /api/projects/[key]/pricing-brief` generates vN+1; `POST /…/[briefId]/apply` flips to applied + writes recommended PSF to `projects.sale_price_per_sqft_override_cents`. UI: `/projects/[key]?tab=pricing` swapped from `PricingTab` to `PricingStrategyTab`. Auto-generates on project create via keepalive fetch from the wizard. Visible to all roles; editor+ can refresh/apply. Migrations `0013_closing_cost_columns.sql` + `0014_pricing_briefs.sql` applied. Legacy `pricing_runs` tables untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-025a hotfixes     | Pricing brief — 7 follow-up fixes                | ✅ Shipped 28-29 May 2026                                    | (1) `0015`/`0016` — `atlas.pricing_briefs` needed GRANT + RLS policies for `authenticated` (editor-write pattern from `pricing_runs`); writes 403'd without them. (2) Model fallback chain in `comp-researcher.ts` + `strategy-brief.ts`: `claude-sonnet-4-5` → `claude-3-7-sonnet-latest` → `claude-3-5-sonnet-latest` (the pinned `claude-3-5-sonnet-20241022` 404'd — deprecated). (3) `reconcileMath()` recomputes every margin/profit/net server-side from Claude's exit prices — LLM arithmetic was wrong (e.g. $7.99M shown as 9% when it's 28.9%). (4) Phase-aware prompt + East End market-context anchor block. (5) UX: honest failed-state card (no fake numbers), 40px headline, integrated Apply CTA, prob-weighted metric only when it differs ≥50bps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| D-026 (V4.14)       | Pricing = market intelligence dashboard          | ✅ Shipped 29 May 2026                                       | `/pricing` rebuilt: action pills (Quick price · Comp library · Pick a project) top-right; unified **Market intelligence** card (one dropdown filter scopes KPIs + bar chart + recent comps; Refresh button fans out AI research across all sub-cuts in parallel and batch-saves); **Active project recommendations** tile grid (moved ABOVE market intel per Viktor). Auto-save AI comps to `atlas.comps` from briefs + quick-price + market-research. New repo fns: `getCompsForDashboard`, `getMarketKpis`, `getPsfBySubCut`, `getRecentClosedComps`, `bulkUpsertCompsIgnoreDupes` (single batch upsert — fixed CF Workers 50-subrequest cap), `listAllCurrentBriefs`. New endpoint `POST /api/pricing/market-research` (optional `{customSubCutLabel}` to research any market — Aspen, Miami, etc). Migrations `0017_comps_dom_days`, `0018_comps_grants` (GRANT footgun again), `0019_comps_waterfront_nullable` (schema drift: drizzle nullable, DB NOT NULL). **Window = 1825 days (5yr)** because AI training-data comps are ~2024-dated while system clock is 2026; honest "Comps from MMM YYYY – MMM YYYY" label + YoY suppressed when either period <3 comps. Avg $/SF tile shows low-high range. JunoThinking widget + pulse on Refresh.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-026 follow-ups    | Pricing dashboard — open                         | 🔲 (a)(c) open · ✅ (b) shipped 31 May 2026                  | (a) Feed library comps to the AI prompt as anchors once library is deep (~30+) so Claude stops re-discovering. **(b) ✅ FIXED 31 May 2026 (commit pending).** Root cause: web search went GA but our code still sent the obsolete `anthropic-beta: web-search-2025-02-14` header → every call 400'd → silently fell through to knowledge-only (`if (status === 400) { /* fall through */ }`). Live DB confirmed 216/217 comps + 3/3 briefs were `used_web_search=false`. Fix: remove the obsolete beta header (web search is now GA — confirmed against current Anthropic docs which show NO beta header on `web_search_20250305`), drop the unnecessary explicit `tool_choice` (auto is the default for server tools), add `max_uses` (5 for comp research, 3 for location classifier — cost cap at $10/1k searches), add `user_location: { country: US, region: New York, timezone: America/New_York }` (East End is hyper-local; sharpens Zillow/Realtor results). Applied to both `comp-researcher.ts::callAnthropic` and `location-classifier.ts::callAnthropic`. Validation: next brief / classify call should persist `used_web_search=true`. (c) Visually distinguish live-MLS vs AI-estimated comps in the feed — now that (b) is fixed, this becomes meaningful.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| D-025b              | Pricing — location factors                       | ✅ Shipped 31 May 2026                                       | Added all 5 factors (`waterfront_type`, `lot_size_acres`, `year_built`, `view_premium`, `town_proximity`) to `atlas.projects` (migration `0021`, CHECK constraints mirror `atlas.comps`; column-add only — existing grants/RLS cover it, but `NOTIFY pgrst`). New `lib/pricing/location-factors.ts` is the single source for the enums, labels, select options, and the AI premium-ladder guidance (waterfront mirrors comps; view+town are project-only). Threaded end-to-end: `ProjectInput`/`ProjectRow`/`projectRowToInput`/repo SELECT, Zod `CreateProjectSchema` + `createProject`, the new-project wizard Basics step, and a read-only "Location & site" card in InputsTab. **Prompts:** both `comp-researcher.ts` (subject's waterfront injected + hard "match the class, never let an off-class comp set the median" criterion; `ResearchedComp` captures `waterfront_type`; market-research classifies it too) and `strategy-brief.ts` (PROJECT block + LOCATION FACTORS section + reasoning rule). Every AI-comp save path stamps `waterfront_type`. **Auto-detect:** `lib/pricing/location-classifier.ts::classifyLocation()` geocodes (Nominatim) + Claude web-search to classify a parcel from its address — wired to a "Detect from address" wizard button (`POST /api/pricing/classify-location`) AND auto-runs in the brief route for any project missing a waterfront class, persisting medium/high-confidence results (`updateProjectLocationFactors`, fill-blanks-only). Data fix: p2 corrected to "84 Sunset Beach Road, Sag Harbor". Backfill of the other live sites happens automatically on next brief (or via Detect). 369 tests (incl. new taxonomy + classifier-parser coverage); the 8 `TBC` placeholder projects stay null (no site). |
| D-029 (V4 fix-pack) | Board-readiness audit fixes (T088-T092)          | ✅ Resolved 1 Jun 2026 (extends to lighthouse fix `ad9cbd3`) | Audit (1 Jun) caught 5 board-blocking gaps: CI never ran (wrong path), security headers not live on Pages Functions, 8 projects with literal "TBC" addresses, atlas.notifications has no committed migration, waterfall+LP not golden-tested. Pre-execution verification workflow CORRECTED V4's prescribed plan on 3 of 5: T089 reject both V4 options (CF Pages doesn't apply \_headers to Functions, next-on-pages doesn't propagate next.config headers() either) — middleware-based headers is the only working path; T090 drop the address_pending column (every consumer already gates on null/truthy — bug is data quality, not schema) + fix p2 seed drift (Springs vs Sunset, south_hampton vs sag_harbor); T091 corrected SQL (column is user_id NOT recipient_id; no CHECK on kind in prod); T092 re-scoped to engine port (no SKIP_KEYS literal exists; no Excel master in repo per D-005; TS aggregator genuinely doesn't compute waterfall/hypothetical_lp/contingency/sales_metrics — that's the actual gap). Execution order: T088 → T091 → T089 → T090 → T092. Per V4 §4.1 the per-ticket PR ceremony is skipped — Viktor's standing workflow is direct commits to main with auto-deploy, "go!" 1 Jun is explicit authorization. ACK_V4.md at atlas/docs/handoff/ captures the deviation.                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-027 (V4.15)       | Pipeline = 3-year velocity workspace             | ✅ Shipped 29 May 2026                                       | `/pipeline` reframed from read-only kanban → goal-driven planner. 3 sections: **Goal tracker** (per-year starts/sells vs target; segmented bars: solid=actual, grey=expected, hollow=gap, +N overflow), **In-flight** (pre-con/construction/sales projects w/ start→sell timeline), **Candidate funnel** (sourcing→sales counts + "need N more starts for {nextYear}" forward signal). Old kanban preserved in collapsed `<details>`. START = `purchase_date` year (actual) else engine `start_date` (expected); SELL = `closing_date` else engine `sale_date`. New Globals `target_starts_per_year` (4) / `target_sells_per_year` (4) / `velocity_plan_years` (3) — migration `0020_velocity_goal_columns`. Pure `lib/services/pipeline-velocity.ts::computeVelocity(inputs, goal, currentYear)`. Editable via **Settings → General → Velocity plan**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## V5.2 sprint (2 Jun 2026) — Strategic Cockpit + Earnings + Ramp-grade Visual Pass

Tag: **`v5.2.0`** · workstream: `atlas/docs/V5_2_TRACKER.md` · plan: `atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V5.md`.

Hard rules respected: no calc-engine changes, no new UI library deps, no Excel-input fields removed. Direct-to-main workflow with auto-deploy.

| ID    | Topic                                                | Status                                      | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----- | ---------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-030 | Closing-costs render as MEMO (no NPAT impact)        | ✅ Shipped 2 Jun (T093.1)                   | The calc engine does not model closing costs or financing fees today (input fields are captured but never deducted in `computePnl`). Decision: the 9-line P&L renders closing as an informational MEMO line that does NOT reduce NPBT, so NPAT stays bit-identical to engine `gross_profit` on every surface. Modelling closing inside the engine is V6. Locked via Viktor's chunk-1 decision; encoded in `lib/finance/project-pnl.ts`.                      |
| D-031 | Funding model: debt-only — no LP equity surfaces     | ✅ Shipped 2 Jun                            | Juno is debt-funded only (KPC LOC + external project finance; no LP equity). UI implication: remove "Equity balance / Equity drawn" series from the per-project cash-flow chart (T094.1); label any residual `equity_drawn` stream as "Owner contribution (often zero)". Owners receive percentages of NPAT, not contributed capital streams.                                                                                                                |
| D-032 | Earnings page scope                                  | ◐ Placeholder 2 Jun                         | Full `/earnings` view (per-owner KPIs, by-project breakdown, 36-mo timeline, distribution log — T097) requires owner↔auth linkage. Decision: ship a placeholder route immediately so the 6-item sidebar is honest; full build unblocks as the other 6 owner login emails are linked (1 of 7 — Viktor — already done).                                                                                                                                       |
| D-033 | Nav consolidation 13→6 + `/analytics` umbrella       | ✅ Shipped 2 Jun (T098)                     | Sidebar collapsed from 15 items / 3 labelled sections (PORTFOLIO / WORKSPACE / ACCOUNT) to 6 primary + 2 account, no section labels. Forecast / Capital / Waterfall / Sensitivity / Scenarios / Stress / Risks become sub-tabs under `/analytics`. 301 redirects in root middleware preserve old bookmarks. AnalyticsTabs component renders the sub-nav at the top of each child page.                                                                       |
| D-034 | Visual commitment tier (committed vs prospect)       | ✅ Shipped 2 Jun (T095)                     | `lib/projects/commitment-tier.ts` is the single source for the rule (real address AND past sourcing). Project cards render with tier-specific treatment: committed = solid stronger border + full opacity; prospect = dashed hairline border + 85% opacity + sunken bg + muted metrics. Pill badges (green "COMMITTED" / amber "PROSPECT") swap in for the old status dot+text. Sort: committed first.                                                       |
| D-035 | Superstructure rename                                | ✅ Shipped 2 Jun (T093)                     | "Kingshaus" / "Prefab" → **"Superstructure"** in all 5 user-facing surfaces (cashflow annual P&L, Actuals tab + client labels, Inputs tab `$/sqft` field, Settings → General default). Engine field name `kingshaus[]` retained internally — engine code untouched (Hard Rule #2). Already-separated stream, so it's a pure rename.                                                                                                                          |
| D-036 | Rollout Profitability Trigger                        | ✅ Shipped 2 Jun (T093.7)                   | New `lib/finance/rollout-trigger.ts`: "When must the next project START to keep trailing-12mo NPAT ≥ exec target?". Pure fn (today_month injected), 6 tests. Backed by 3 new `atlas.globals` columns (mig 0025): `target_annual_npat_usd`, `fixed_overhead_annual_usd`, `project_time_to_npat_months` (default 18). Surfaced on Summary tab + Dashboard "Rollout pacing" chip. NPAT target live at **$8M** (Viktor confirmed 2 Jun).                         |
| D-037 | Strategic dashboard chips reframe                    | ✅ Shipped 2 Jun (T096)                     | `/dashboard` rebuilt: Row 1 = 4 strategic chips (Next capital call · Next owner distribution · KPC LOC headroom · Rollout pacing) answering exec board questions directly. Row 2 = 3 tactical chips demoted to secondary weight. Row 3 = 3 action cards. Row 4 = 12mo portfolio flows chart. Row 5 = top-2 committed projects. Old 6-KPI tile + "Recent projects" sidebar removed.                                                                           |
| D-038 | Capital sources seed table                           | ✅ Shipped 2 Jun (T096.1b)                  | New `atlas.capital_sources` table (mig 0027) — minimal `kpc_loc / project_finance / recycled_equity` rows with limit/drawn/rate. Seeded: **KPC Family Office LOC, $6M @ 6%, $0 drawn** (Viktor confirmed 2 Jun). Drives the KPC LOC headroom chip. Full ledger (rates, covenants, draw schedules) is V6 treasury layer.                                                                                                                                      |
| D-039 | UX/UI principles §3a codified as PR review gate      | ✅ Shipped 2 Jun (T103.4/.7)                | New docs `UI_VOCABULARY.md` (verb-first label table + font-weight rule + list treatment), `TOOLTIPS.md` (plain-English hover copy), `COLOR_TOKENS.md` (state palette + monochrome chart palette + multi-color exceptions + lime discipline table). Reviewers check against these going forward.                                                                                                                                                              |
| D-040 | T099 + T101 folded into T103                         | ✅ Shipped 2 Jun                            | T099 (Actuals empty-state — red variance numbers on unstarted projects) subsumed by T103.3's empty-state audit pass: "No cost entries yet" muted banner shown when `entriesTotal === 0`. T101 (approval banner → header chip) subsumed by T103.2: wide banner → compact red/amber/green pill chip with click-to-expand drawer carrying Create/Refresh/Lock/Approve.                                                                                          |
| D-041 | Sand brand tone (Juno differentiator vs Ramp's grey) | ✅ Shipped 2 Jun (T103.11)                  | New tokens `--color-brand-sand` (#E8DFCC), `--color-brand-sand-soft` (#F2ECDC), `--color-brand-sand-strong` (#D4C7AD). Reserved for hero/sign-in surfaces only — NEVER on data UI. Initially shipped as sign-in bg; 2 Jun visual feedback reverted sign-in to white (Render.com-style) but tokens preserved for future hero surfaces (V6 distribution-forecast page candidate).                                                                              |
| D-042 | Sign-in dot-grid signature                           | ✅ Shipped 2 Jun (T103.11 + feedback round) | Vanilla JS + Canvas 2D, no library. Initial sand variant `3739cd4`; iterated 2 Jun per feedback → white bg + smaller grey dots (1.1px base / 2.4px peak / 22px spacing) + **magnetic gravity-well displacement** (dots within 140px pulled toward cursor on cubic falloff, max 14px). prefers-reduced-motion → single static render; canvas is z-0, pointer-events:none, aria-hidden. Lighthouse perf score unchanged.                                       |
| D-043 | Platform-wide Ramp-grade visual pass                 | ✅ Shipped 2 Jun (T103.1/.8/.9/.10)         | Format helpers `lib/format/{money,percent,date}.ts` (canonical formatters). **Two-weight system** enforced: zero `fontWeight: 500/600` remain across 60+ files (all → 400 or 700). Pure-white page background (`--color-surface-base` via layout.css). Monochrome chart palette tokens (`--chart-default-{1..4}` + axis/gridline) — portfolio cash-flow chart migrated to mono; per-project flows chart retained as functional-color exception (documented). |

---

## V6.1 sprint (2 Jun 2026) — Editable Platform + Home/Projects/Pipeline UX + Ask Juno Agent

Tag: **`v6.1.0`** (Part 1 milestone `v6.1.0-beta.1`) · workstream: `atlas/docs/V6_1_TRACKER.md` · plan: `atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V6_1.md`.

> **Decision IDs shifted +1 from the plan (DR-1):** the plan assigned V6.1 `D-043 → D-055`, but `D-043` was already taken by V5.2's Ramp-grade visual pass. V6.1 therefore uses **`D-044 → D-056`**.

| ID    | Topic                                                                                    | Status                  | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ---------------------------------------------------------------------------------------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-044 | Editable inputs via modal + `PATCH /api/projects/[id]`                                   | ✅ Shipped 2 Jun (T104) | One modal (Schedule · Villa · Costs · Financing · Targets · Tax) → `PATCH /api/projects/[id]`, E1-gated (requireAuth → requireEditor → Zod → audit). Save runs the engine FIRST (E7: a calc throw surfaces verbatim as `CALC_ENGINE_ERROR` 422 and nothing persists), then version-bumps + writes one audit row with full before/after JSONB tagged `source='ui'`. **Versioning deviation vs §3b E4:** the current row's UUID stays STABLE (update-in-place + archive the prior state as a new historical row) because `approval_snapshots.project_id` FKs a specific version UUID and the immutability trigger forbids repointing locked snapshots — a fresh UUID would orphan the re-approval gate. **Re-approval gate (E3):** locked snapshot + drifted editable inputs → header chip flips to amber "Re-approval needed" (`lib/projects/reapproval.ts`; placeholder for the T113 StatusDot). Migrations **0029** (projects `last_edited_*`/`edit_source`) + **0030** (audit_log `source` ONLY — before/after already existed, DR-3). **Gaps surfaced not fabricated:** `kingshaus_cost_per_sqft` + `ltc_pct` are in the ProjectInput type but have NO project column → excluded from the editor (T104 stop-and-ask). `UpdateProjectSchema` lives in `lib/services/project-schema.ts` (not the plan's `lib/schemas/project.ts`) and is fully partial (T109 field-scoped reuse). Dirty-check confirm composed from `<Modal>` (no `<ConfirmDialog>` exists — E6). Engine untouched; golden 23/23, full suite 426/426.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-045 | Monthly P&L on Summary tab + dynamic chart timeline platform-wide                        | ✅ Shipped 2 Jun (T105) | New `lib/charts/project-window.ts` (3 helpers: `projectWindow` per-project, `portfolioWindow` aggregate, `autoWindow` activity-based). New `buildProjectPnLMonthly` appended to `lib/finance/project-pnl.ts` — pure presentation: reads existing `MonthlySeries` arrays without recomputing the engine (Hard Rule #2). New client component `monthly-pnl-table.tsx` with sticky-left labels (200px), horizontally-scrollable months (72px each), sticky-right totals column (88px), and "Show full model horizon" toggle defaulting OFF (project window only). Golden test (11 cases) proves each monthly column sums to `buildProjectPnL()` totals within $1. **Per-month NPAT consistency:** sum(monthly NPAT) ≥ pnl.NPAT when individual loss months exist because per-month tax floors at 0; documented in the test as the intentional invariant. `CashFlowChart` + `PortfolioCashFlowChart` trim to default windows with matching toggles. 444/444 tests, typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-053 | `<StatusDot>` pulsating-dot primitive replaces inline alert banners (DR-5 resolved)      | ✅ Shipped 2 Jun (T113) | New `components/feedback/StatusDot.tsx` (client) + `feedback.css` `@keyframes status-dot-pulse`. 6px dot, 2s opacity-pulse (1.0 ↔ 0.55), `prefers-reduced-motion: reduce` disables animation. Hover/focus opens popover (≤320px, hairline border, triangle caret) with title + message + optional action button + relative timestamp. `suppressIfZero` prop renders nothing when delta === 0 or non-finite (kills KPI YoY zero-clutter). `StatusDotGroup` is a flex wrapper today; "+N more" collapse marked TODO for V6.1 v2. **DR-5 resolved: reused existing `COLOR_TOKENS` palette** (warning #a16207, error #b91c1c, info #0D0D0D) rather than the plan's slightly different hexes (#D4A017, #C0392B), for platform consistency — Viktor iterated the palette 3× in V5.2. Swept 3 offenders: `/pricing` stale-data banner (D-026 hold-over) → amber dot; `KpiTile` YoY delta tags → dot+suppress; project re-approval chip (T104 placeholder) → real StatusDot. 7 unit tests; 433/433 full suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-046 | Assumptions hero block + cash-flow debt-outstanding overlay                              | ✅ Shipped 3 Jun (T106) | New `AssumptionsHero` on Summary tab above the 9-line P&L — 3-column × 4-row KPI grid mirroring the Excel "Assumptions and key figures" K6:M31 block. Rows: (1) Start date · Sale date · Total villa (with AG+BG split); (2) $/sqft target · $/sqft actual · NPAT margin (with target sub); (3) Land cost · Construction $/sqft · Superstructure $/sqft; (4) Senior LTV · Interest rate · Total financing cost (with peak debt sub). Every cell reads from `ProjectInput` or `ProjectKpis` — no hardcoded numbers, no fabricated values. Sale date from `ProjectResult.sale_date` (engine output). Where a value is null (e.g. `build_cost_per_sqft` falls back to globals), the cell shows "—" with a "using global default" sub. **Cash-flow overlay:** `debt_outstanding` added to `CashFlowMonth` (reads existing engine `debt_balance` — no engine change), rendered on `CashFlowChart` as a monochrome dashed Line (`#4B4B48`, dash 4-3). Reconciliation invariant tested: overlay value at month N === `debtSnapshotForMonth(m, dates[N]).debt_outstanding` (both read `m.debt_balance` directly). 445/445 tests; typecheck + lint clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| D-047 | Structured cost breakdown via optional JSONB column (engine untouched)                   | ✅ Shipped 3 Jun (T107) | New `cost_breakdown jsonb` column on `atlas.projects` (migration 0031 — additive only). Zod schema `CostBreakdownSchema` validates 4 category groups (construction · superstructure · soft · financing); each line item has label, amount_usd, optional note, optional status (estimate / committed / paid). Engine still reads ONLY the lump-sum fields (`build_cost_per_sqft × villa_sqft_ag`, `soft_costs_lump_sum`, etc.) — Hard Rule #2: golden 23/23 prove the breakdown column is invisible to `runProject`. Inputs editor gains a 7th `<Section>` with 4 collapsible `<details>` groups + inline add/delete rows + status `<select>` + per-group divergence `<StatusDot severity="warning">` fired when itemized total diverges > 5% from the lump-sum mirror (Construction reconciles vs `build_cost_per_sqft × villa_sqft_ag`; Soft reconciles vs `soft_costs_lump_sum`; Superstructure + Financing have no lump-sum mirror in the editor → no dot, just sum). **Deviations:** (V6-05.a) plan calls it a "new tab", but the existing modal uses stacked `<Section>` blocks — shipped as a 7th Section. (V6-05.b) `ProjectInput.cost_breakdown` uses an inline `CostBreakdownShape` interface declared in `lib/calc/project/types.ts` rather than importing the Zod-inferred type from `lib/services/project-schema.ts`, to keep the calc-engine module free of service-layer / Zod imports. Structurally identical; the editor casts via `as unknown as CostBreakdown` once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-048 | LLM-assisted CSV import for Actuals (Claude Sonnet 4.5, stateless round-trip)            | ✅ Shipped 3 Jun (T108) | New `POST /api/projects/[id]/actuals/import` (nodejs runtime, E1-gated, `source='csv_import'` in audit). Two phases: **dry_run** parses CSV via the new zero-dep `lib/csv/parse.ts` (RFC-4180-ish, BOM-strip, quoted-with-newlines, escaped quotes, mixed CRLF/LF, padded short rows), then calls Sonnet 4.5 to map source columns onto `{date, vendor, category, amount_usd, description, project_key}`, then validates every row and returns a preview + `validated_rows` carried back to the client. **commit** receives the validated rows, re-validates each one server-side (defense-in-depth — never trusts the client), and bulk-inserts via a single supabase `.insert([...])` (Postgres aborts the batch on any constraint failure = spirit of rollback the plan calls for). One audit row written per import with file_name + row_count + total_amount + first/last entry id. Anthropic via raw `fetch()` (no SDK — Hard Rule #3), mirroring `comp-researcher.ts`; model fallback chain `claude-sonnet-4-5 → claude-3-7-sonnet-latest → claude-3-5-sonnet-latest`; 404 → next model, 401/403 → 502 stop, otherwise 502 verbatim (NO silent degradation per §T108 stop-and-ask #3). Per-row category normalization: enum-exact or known synonym (Hard costs → build, Architect → soft, etc.); unknown → row marked invalid with reason "category 'X' not in allowed enum" (NEVER fabricates §T108 stop-and-ask #2). New 3-step Modal client component (Upload / Preview / Success) with drag-drop, confidence dot (green ≥0.85 / amber 0.5–0.85 / red < 0.5), column-mapping table, warnings list, first 20 rows with ok/invalid badges + inline errors per row. **Stateless server:** Cloudflare Pages Functions don't preserve memory between requests → validated rows round-trip in the response body. **Deviation V6-06.a:** plan accepts `.xlsx`, but the repo has no XLSX parser and adding one violates Hard Rule #3 — shipped CSV-only with a 400 error on `.xlsx` redirecting users to "Excel → Save As → CSV". XLSX promoted to V6.1 v2. 14 new tests (parse 8, fetch-mocked column-mapper 6); 459/459 full suite. |
| D-049 | Platform-wide editability complete (Sales / Risks / Timeline / Actuals CRUD + stage=tbc) | ✅ Shipped 3 Jun (T109) | Mig 0032 (`atlas.project_risks` CRUD table + RLS) applied. New `lib/repos/project-risks.ts` + `GET/POST /api/projects/[id]/risks` + `PATCH/DELETE /api/risks/[id]` + `RisksClient` UI (inline add/edit/delete). `SalesEditorModal` (sale_price_override + psf + margin via existing PATCH endpoint). `DELETE /api/actuals/[id]` (soft-archive). `stage='tbc'` added to `CreateProjectSchema` enum and set as create default. Projects list `+ New project` button gated on `isEditor`. STAGE_FILTERS gain TBC chip. **Timeline date edits** covered by the existing Inputs editor (purchase_date + duration months); separate Timeline tab editor deferred — same fields, same PATCH route. **Actuals row PATCH** deferred — bulk LLM-import (T108) handles the typical use case; single-row edit is V6.1 v2. **Create modal for the projects list page** = the existing full-page `/projects/new` wizard (DR-4: it already exists and is E1-gated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| D-050 | Home redesign + forecast page merged into Home                                           | ✅ Shipped 3 Jun (T110) | Two-column Home: 60% Boardroom Strip (4 strategic rows — Next capital call · Next owner distribution · KPC LOC headroom · Rollout pacing) + 40% Today's Desk (drafts to lock · capital calls drafting · cap-breach months). 3-cell tactical strip below Boardroom (90d cash · Pipeline rev · Starts {year}). Below both columns: `PortfolioCashFlowChart`, then `AnnualPnLTable` promoted from `/analytics/forecast/page.tsx` (which is deleted). 301 redirect `/analytics/forecast → /dashboard` added to root middleware. Legacy `Chip` / `SmallChip` / `ActionCard` components removed. **Deviation V6-07.a:** plan §2.7 says "12-month portfolio cash-flow chart below both columns" — kept the existing `PortfolioCashFlowChart` (49-month max horizon, trimmed by `autoWindow` from T105). Reframing to a strict 12-month cut requires changing `aggregatePortfolio` boundaries, which is a calc-engine change (Hard Rule #2 violation) — deferred. **Deviation V6-07.b:** capital-call-overdue StatusDot wiring on the Boardroom row is folded into the existing color-prop guards (`overdue` / `warn`) — no separate dot element until the engine emits an explicit `is_overdue` flag. Effective tax rate is computed from active globals (federal + state) rather than passed in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-051 | Projects list redesign (Table + Map placeholder + Timeline)                              | ✅ Shipped 3 Jun (T111) | Sortable 7-column table (Project · Stage · $/sqft target · Total revenue · Margin · Sale month · Owner) as default view; sticky header; click row → detail. Above the table: stage filter chips (kept per Viktor's V5.2 preference) + 3-button view toggle (Table / Map / Timeline). View choice persists in localStorage as `juno_atlas_projects_view`. Timeline view = div-based Gantt bars (one bar per project, stage-coloured by month band). **Deviation V6-07.c:** plan §2.8 + §T111.3 says "Map view uses MapLibre GL (already in stack via comp researcher)" — `package.json` grep confirms MapLibre is NOT in the stack. Shipped a "Map view (V6.1 v2)" placeholder card; the real map requires `maplibre-gl` and `mapbox-style-spec`, both new packages = Hard Rule #3 violation. Defer to V6.1 v2 unless Viktor explicitly approves the new deps. `ProjectRowVM` extended with `sale_price_per_sqft_override`, `start_date`, `sale_date` (read from `ProjectResult` not `ProjectInput` — the `Input` type has no `sale_date`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-052 | Pipeline editability — native HTML5 drag-and-drop + velocity goal editor                 | ✅ Shipped 3 Jun (T112) | `pipeline-board.tsx → 'use client'` + native HTML5 drag-and-drop (no react-beautiful-dnd, no dnd-kit — Hard Rule #3). Each card `draggable={isEditor}`; column `onDragOver` (preventDefault) + `onDrop` calls `PATCH /api/projects/{key}` with `{ stage: newKey }`. Optimistic UI update; on failure, revert + inline "Move failed" red badge per card. New `PATCH /api/globals/velocity` endpoint (E1-gated, Zod-validated). `GoalTracker` gains an "Edit goals" toggle for editors that swaps into an inline 3-input form (starts/year, sells/year, plan years) — saves call the new endpoint then `router.refresh()`. **Editor-gated:** drag handles and edit buttons hidden for viewer roles. **Decision id D-052** per DR-1 numbering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| D-054 | "Finance & Analytics" rename + Activity timeline compression                             | ✅ Shipped 3 Jun (T114) | Sidebar label `Analytics → Finance & Analytics` (1-line edit in `patterns/AppShell.tsx`). Route stays `/analytics`. All 6 sub-tabs unchanged (`/analytics/{capital,waterfall,sensitivity,scenarios,stress,risks}`). Activity tab compressed: was a 4-column grid (170px timestamp / 90px category / content / status); now a tight 2-line vertical-timeline row — line 1 = timestamp · actor · category · fail badge (12px), line 2 = action + resource + meta (13px); 6px row padding; hairline divider per entry. GitHub-style. AppShell test updated for the new label. **Decision id D-054.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| D-055 | Ask Juno tool-calling agent v1 ($10K low-risk threshold)                                 | ✅ Shipped 3 Jun (T115) | New `lib/ask-juno/system-prompt.ts` (Atlas §−1 purpose + 7-owner cap table + 6 strategic questions + E1-E10 + low-risk rules + audit-log-id instruction). New `lib/ask-juno/risk-classifier.ts` (auto-execute gates: tool in LOW_RISK_ELIGIBLE set, INSERT only, ≤$10K monetary impact, batch size <5, no locked snapshot, editor+ role — defaults to confirmation when unsure). New `lib/ask-juno/tools.ts` (8 tools: 4 READ — list_projects, get_project_summary, get_dashboard_kpis, search_actuals; 4 WRITE — create_project, update_project, create_actuals_entry, create_risk; READ executes immediately, WRITE goes through risk classifier). `POST /api/ask-juno` rewired to Node runtime: build messages, call Anthropic with tools parameter, dispatch READ tools, classify WRITE tools, return `pending_confirmation` or auto-execute, feed tool_result back to Claude for final reply. AskJunoWidget client updated with confirmation cards (Confirm/Cancel/Executing states), tool execution status rows, and audit log id display on completed writes. **Deviation V6-08.a:** Vercel AI SDK NOT in package.json (plan §T115.1 said "via pricing brief"; grep confirms zero AI SDK refs). Using raw Anthropic Messages API `tools` parameter (same fetch pattern as `comp-researcher.ts`) — equivalent functionality, no new packages. **Deviation V6-08.b:** per-owner read-row visibility (B-2 blocker, 6/7 owners unlinked) deferred to V6.3 — current implementation enforces ROLE-based write gating, sufficient for T115's scope. 20 unit tests for `classifyRisk`; full suite 479/479 (golden 23/23).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| D-056 | Ask Juno file ingestion v2 (CSV via parseCsv, PDF via Sonnet 4.5 document input)         | ✅ Shipped 3 Jun (T116) | New `POST /api/ask-juno/ingest` (Node runtime, 60s timeout, editor+ gated, 10 MB hard cap). CSV files routed through T108's `lib/csv/parse.ts` (zero-dep RFC-4180-ish); first 50k chars + header + 20 sample rows fed to Sonnet 4.5 with model fallback chain. PDF files base64-encoded as `document` content block (Anthropic's native PDF input mode). DOCX falls through to plain text extraction. Server returns `ingest_proposal` with file_name, confidence (0–1), line_items array, by_category summary, uncategorized_count, total_usd, and warnings. AskJunoWidget gains a 📎 file input button next to Send; proposal renders as an assistant chat message with the summary + redirect to "review in the Actuals tab to commit". **Always-confirm:** file ingestion never auto-executes per §2.6; the user reviews the proposal then goes to the project's Actuals → Import CSV flow to commit (reuses T108's batch insert path — no parallel implementation, per §T116.5). **Deviation V6-09.a:** XLSX rejected with 400 `XLSX_NOT_SUPPORTED` and helpful "Excel → Save As → CSV" redirect (same V6-06.a basis). **Deviation V6-09.b:** DOCX uses plain text extraction (`file.text()`) rather than a proper DOCX parser — works for simple text-only docs; rich formatting, tables, embedded images are lost. Promoted as a V6.1 v2 follow-up; ingestion is always-confirm so the user catches extraction errors before commit. Decision id `D-056`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## V6.2 sprint (3 Jun 2026) — Treasury Layer

Tag: **`v6.2.0`** (Part 1 milestone `v6.2.0-beta.1`) · workstream: `atlas/docs/V6_2_TRACKER.md` · plan: `atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V6_2.md`.

| ID    | Topic                                                                                                                   | Status                  | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-065 | Boardroom reconciliation — one treasury schedule feeds every strategic answer                                           | ✅ Shipped 3 Jun (T126) | Enforces the V6.2 thesis (TR7 — "no surface independently recomputes a treasury number"). Dashboard refactored: builds ONE `buildCashSchedule` and derives both **Next capital call** (first `net_cash_need > 0` row — replacing the V5.2 committed-only `debt_drawn` scan over a separate engine pass) and **Next owner distribution** (`buildDistributionForecast` first month — replacing `buildProjectPnL` of the next committed close) from that single schedule, so the chips reconcile with `/analytics/cash-schedule` + `/earnings` by construction. All five Boardroom row links re-pointed to the surface that owns each answer's math (capital call→cash-schedule, distribution→earnings, LOC headroom→loc, rollout→pipeline/capacity, self-funding→self-funding). **Reconciliation suite** `lib/treasury/__tests__/reconciliation.test.ts` builds one real schedule from the 10 vanilla fixtures and asserts the three plan §T126.2 invariants: (1) `buildLocRepayment` outstanding[M] ≡ schedule `kpc_loc.balance_eom[M]` for all 36 months; (2) `buildDistributionForecast` annual total[FY] ≡ `buildSelfFundingTrajectory` `annual_distributions[FY]`; (3) self-funding `annual_equity_need[FY]` ≡ Σ schedule `net_equity_drawn` over FY, and the Boardroom next-capital-call ≡ the first net-draw row. These guard against future drift where one function changes but its consumers don't. **Note:** the dashboard's "next capital call" is now portfolio-wide (the canonical schedule number) rather than committed-only — the intended consequence of single-source reconciliation. Engine untouched. typecheck + lint + preflight + build clean; 545/545 tests, golden 23/23.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| D-064 | Distribution Forecast — owner tax distribution + email-match per-owner visibility                                       | ✅ Shipped 3 Jun (T125) | New `lib/treasury/distribution-forecast.ts`: pure `buildDistributionForecast(schedule, capTable, opts?)` returns `monthly[]` + `annual{}` per-owner distributions, owner metadata, blended `distribution_rate`, `insufficient_data`. **Distribution model:** per Viktor's D-062 confirmation ("owner tax only"), each owner's distribution = `NPAT_month × share_fraction × tax_rate_fraction` (their tax distribution); monthly total = `NPAT × blended rate`. **This supersedes the literal T125 spec** ("owner share by cap_table bps" = 100% of NPAT) so every treasury surface uses ONE distribution policy — the annual total reconciles with the self-funding `annual_distributions` (golden reconciliation test). Overridable via `opts.distributionRate` (applied pro-rata by share). **`/earnings` replaces the V5.2 placeholder:** hero (FY total / your share / blended rate), 36-month monthly column chart, annual per-owner table. **§2.7 per-owner visibility — VB-3 workaround:** `atlas.owners` has no `user_id` (the owner↔auth link Viktor still owes), so `resolveOwnerByEmail` bridges on email — super_admin sees all owner rows; a logged-in owner whose email matches a cap-table row sees their own row + portfolio total; everyone else sees the total + "Pending account" and unlinked owners render "pending account" in the admin view. Also wired the Scenario Modeler's 6th answer (previously "Pending") to the live next-distribution. **Window is forward-only** (the cash schedule has no trailing history; realised distributions come from the books, not this forecast). NPAT pre-tax proxy carried forward (V6-13.a). 10 unit tests. Engine untouched. typecheck + lint + preflight + build clean; 540/540 tests, golden 23/23.                                                                                                                                                                                                                                                                                                                                                                                           |
| D-063 | Scenario Modeler — client-side live recompute of strategic answers                                                      | ✅ Shipped 3 Jun (T124) | New `/analytics/scenario-modeler`. Migration 0035 adds `starts_per_year_override` (int, nullable, ≤50) to `atlas.scenarios`; `lib/repos/scenarios.ts` (ScenarioView/Row/Input + ROW_SELECT + normalize) and the POST + PATCH `/api/scenarios` Zod schemas extended (insert/update spread the field automatically). **Recompute architecture (per plan §T124.2):** the modeler does NOT round-trip to `/api/scenarios/preview` like the V4 scenario-comparison page — it recomputes LOCALLY in the browser. The server page passes the raw data set (projects+inputs, sources, assignments, cap table, globals, active scenario) to a `'use client'` island; on each slider change (200ms-debounced `useMemo`) it builds a `Scenario` and runs `buildCashSchedule` + `runProject` + `solveStartCapacity` + `buildSelfFundingTrajectory` + `computeRolloutTrigger` — the exact same pure functions the dedicated pages call, so the answers reconcile by construction. **Client-safety verified with a real `next build`:** all five calc/treasury modules are pure (no `@/lib/supabase/server`, `next/headers`, or `server-only` value imports); the one repo type (`CapTableEntryView` in self-funding) is `import type` (erased at build). 5 sliders: sale-price ×, build-cost ×, interest-rate Δ bps, timing shift, starts/year. The first four flow through the engine into every answer; **starts/year is a forward-planning knob** persisted with the scenario but not fed to `runProject` (the modeler recomputes over the existing project set). Save → POST `/api/scenarios`; "Save & activate" → also POST `/api/scenarios/active` (flips the `atlas-active-scenario` cookie). New "Modeler" analytics sub-tab. **6th answer (owner distribution) shows "Pending"** — `buildDistributionForecast` lands in T125 (blocked on VB-3). Engine untouched. typecheck + lint + preflight + build clean; 530/530 tests, golden 23/23.                                                                                                                                                                                                                             |
| D-062 | Self-Funding Trajectory — distribution modelled as blended owner tax rate (✅ Viktor-confirmed 3 Jun: "owner tax only") | ✅ Shipped 3 Jun (T123) | New `lib/treasury/self-funding.ts` exposes pure `buildSelfFundingTrajectory(schedule, capTable, opts?)` returning per-FY `annual_npat`, `annual_distributions`, `annual_retained_npat`, `annual_equity_need`, plus `self_funding_year` (first FY retained ≥ need, need > 0), `years_to_self_funding`, `distribution_rate`, `insufficient_data`. Reads the D-059 cash schedule — added an **additive** `net_equity_drawn` field to `CashScheduleRow` (populated from the engine's `monthly.equity_drawn`; golden parity unaffected — the parity test asserts draw/repaid/balance, not the new field). **Distribution policy decision:** "retained after owner distributions" requires a distribution rate, which Atlas has NOT configured anywhere. Rather than invent one, the DEFAULT distribution = the _owner tax distribution_ — `distribution_rate = Σ_owners (share_fraction × tax_rate_fraction)` from the cap table (`share_bps` + each owner's `tax_rate_bps`). This is the cash owners must receive to pay taxes on their allocated NPAT; it's derived ENTIRELY from configured cap-table data (nothing invented). `retained = NPAT − NPAT × rate`. Overridable via `opts.distributionRate` (T124 scenario modeler passes an explicit rate). Empty cap table → rate 0 → all retained (safe degenerate). **Surface** `/analytics/self-funding`: hero ("Self-funding by {FY}" or "Insufficient data" when no NPAT in horizon), side-by-side retained-vs-need bar chart (recharts) with a reference line on the self-funding FY, annual detail table. New "Self-funding" analytics sub-tab + **5th Boardroom row** on the dashboard (same treasury pipeline → reconciles by construction). 10 unit tests. **Known limitation:** the schedule's `net_profit_after_tax` is still a pre-tax proxy (carries V6-13.a); the blended-tax distribution approximates the after-tax leakage so retained ≈ true after-tax retained earnings — T125 will layer true per-project tax + Viktor confirms the distribution policy. Engine untouched.                                                                                                                        |
| D-061 | Start Capacity Solver — covenant-limited concurrency, never invents the cap                                             | ✅ Shipped 3 Jun (T122) | New `lib/treasury/start-capacity.ts` exposes pure `solveStartCapacity(schedule)` returning `{state, max_concurrent_starts_now, next_available_start_month, covenant_max_concurrent_projects, peak_concurrent_projects, min_headroom_usd, rationale}`. Reads the SAME `buildCashSchedule` output (D-059) — no engine recompute, reconciles with the cash schedule + LOC pages by construction (TR7). **Formula:** for each month M, `slack(M) = covenant_max_concurrent_projects − concurrent(M)` where `concurrent(M)` = distinct projects with LOC debt that month; `max_concurrent_starts_now = max(0, min slack over the near-term 6-month window)`; `next_available_start_month` = first month with positive slack when at capacity now. **BLOCKED-ON-VIKTOR (VB-1):** when the KPC LOC has no `covenant_max_concurrent_projects` set, the solver returns `state: 'unconfigured'` with an actionable "set the covenant in Settings → Capital Sources" message — it does **not** invent a cap (nothing ships to prod with invented numbers). Also `unconfigured` when no kpc_loc source exists. **Surface** `/pipeline/capacity` — hero integer + next-start month + rationale paragraph + 36-month concurrency-vs-ceiling step chart (recharts). Pipeline page (V6.1) gets a "Capacity" chip linking through, showing the integer when configured. 7 unit tests (unconfigured/no-LOC/plenty-of-slack/at-capacity-opens-later/exactly-at-cap/one-slot/never-opens). Engine untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| D-060 | KPC LOC repayment schedule — first-paydown + full-clearance from cash schedule                                          | ✅ Shipped 3 Jun (T121) | New `lib/treasury/loc-repayment.ts` exposes pure `buildLocRepayment(schedule)` returning `{source_id, source_name, first_paydown_month, full_clearance_month, months_to_full_clearance, peak_outstanding, timeline[]}`. Pure derivation from the D-059 cash schedule's per-source slices — no engine recompute. **first_paydown_month** = first month LOC outstanding strictly decreases after having been positive; **full_clearance_month** = first month outstanding returns to ≤$1 (CLEARED_EPSILON float tolerance) after peak > epsilon; **timeline** carries per-month outstanding + `interest_accrued = outstanding × (annual_rate/12)`. Returns empty result (null source) when no kpc_loc configured. **Surface** `/analytics/loc` — two hero numbers (first paydown · full clearance) + peak-outstanding + 36-month outstanding-balance area chart with reference-line annotations at both milestone months (recharts). New "LOC repayment" sub-tab in Finance & Analytics. 6 unit tests (never-drawn/single-paydown-then-clear/multi-paydown/never-cleared/interest-accrual/no-source). Engine untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| D-059 | Portfolio 36-month cash schedule — single source of truth for V6.2 Part 2                                               | ✅ Shipped 3 Jun (T120) | New `lib/treasury/portfolio-cash-schedule.ts` exposes pure `buildCashSchedule({projects, globals, scenario, sources, assignments, todayYM})` returning 36 rows from today + 35 months. **Allocation algorithm — priority-ordered greedy:** each project walks its source assignments in priority order (`0` drawn first); each month, draw up to source remaining-headroom, spill to next on overflow. Repayments reverse-priority (lowest-priority paid down first — pay down expensive senior debt before LOC). Empty assignment stack defaults to `[kpc_loc]` (V6.1 back-compat). Engine called once per project via `runProject`, never recomputed — Hard Rule #2 preserved (golden 23/23 unchanged). **Covenant module:** new `lib/treasury/covenants.ts` (Hard Rule #6 — every check is a pure function with a JSDoc formula + golden test at threshold/below/above): `checkMaxLtcCovenant` (outstanding/total-cost ≤ ceiling), `checkMaxConcurrentProjectsCovenant` (distinct active-debt projects ≤ ceiling, INCLUSIVE), `checkDrawWindow` (month within configured date range). Null covenant = never breaches. Per-project total-cost for the LTC denominator = `kpis.total_cost_all_in`. **Test coverage:** 28 new tests — 17 covenant (3-case threshold matrix for each) + 11 cash-schedule (parity vs engine: sum per-source drawn[M] === sum project debt_drawn[M] within 50¢; same for repaid; balance_eom = starting_drawn + cum(drawn − repaid); breach detection on capped-concurrent source; fallback-to-kpc-loc when no assignments). **Surface** `/analytics/cash-schedule` — server component reads everything in parallel (5 promises), pure aggregator runs ~1ms, wide horizontally-scrolling client table with sticky left (month) + sticky right (StatusDot for breach popovers) + collapsible balance/headroom columns. New sub-tab "Cash schedule" in Finance & Analytics (`atlas/app/_components/analytics-tabs.tsx`). 507/507 tests; typecheck + lint + preflight clean.                                                                                                                                                             |
| D-058 | LOC priority order configurable per project (drag-reorder)                                                              | ✅ Shipped 3 Jun (T119) | New `CapitalSourcesEditor` standalone client island on the project Inputs tab — separate from the InputsEditor modal because (a) the assignments persist to a different endpoint (`PUT /api/projects/[id]/capital-sources`, T118-built) and (b) keeping it independent avoids two-PATH save coupling (one save can succeed while the other fails). Native HTML5 drag-and-drop, same pattern as T112's pipeline-board — no dnd-kit, no react-beautiful-dnd (Hard Rule #3). Lazy fetch on first Open: `GET /api/capital-sources` (active sources for the dropdown) and `GET /api/projects/[id]/capital-sources` (current assignments) in parallel. Dropdown shows only unassigned sources; × removes; drag swaps positions (priority = array index, 0 drawn first). Save runs only the PUT — independent from the InputsEditor modal's PATCH save. Empty stack = T120 aggregator default fallback (KPC LOC only). Editor-gated; viewers see nothing. **Per-source covenants surface inline:** each row shows max LTC + max concurrent projects when set, so the editor sees facility constraints while ordering. Engine untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| D-057 | Capital Sources versioned ledger + per-project assignments                                                              | ✅ Shipped 3 Jun (T118) | Migration 0033 extends `atlas.capital_sources` from V5.2's flat seed shape to the full ledger: `covenant_max_ltc_pct` (decimal), `covenant_max_concurrent_projects` (int), `draw_window_start_date` + `draw_window_end_date` (date), `priority_order` (int, lower drawn first), plus the versioning model — `version` + `is_current` + `is_archived` + `created_by` — mirroring `atlas.projects`. Partial unique index `atlas_capital_sources_kind_current_unique` enforces "one current row per (source_kind, source_name)" at the DB level. Migration 0034 creates `atlas.capital_source_assignments` (project_id × capital_source_id, ordered by `priority`) — the per-project funding stack. Repo `lib/repos/capital-sources.ts` exposes `findActiveCapitalSources`, `findActiveKpcLoc`, `findCapitalSourceById`, `findAssignmentsForProject`, `insertCapitalSourceVersion`, `archiveCapitalSource`, `setAssignments`. Service `lib/services/capital-sources.ts` wraps with E1 audit (`source='ui'`). Three API routes (edge runtime): `GET/POST /api/capital-sources` (super_admin POST), `PATCH/DELETE /api/capital-sources/[id]` (super_admin), `GET/PUT /api/projects/[id]/capital-sources` (editor+ PUT). New Settings tab "Capital Sources" — super_admin only — with table view + modal editor (versioned save: each PATCH mints a new version row). Dashboard KPC LOC chip rewired from raw supabase query to `findActiveKpcLoc()` — respects `is_current`/`is_archived` filters (so archived KPC seed rows can't poison the chip). **Versioning model:** UPDATE writes a new row with `version+1`, demotes prior `is_current` to false; ARCHIVE flips `is_archived=true` AND `is_current=false`; create-on-failure rolls back the prior demote. Engine untouched. 479/479 tests, golden 23/23 green; typecheck + lint + preflight clean. **Seed-data BLOCKED-ON-VIKTOR:** the existing KPC LOC seed row from V5.2 mig 0027 doesn't have covenants populated; Viktor enters them via Settings → Capital Sources when he has the term sheet (no code change needed). Same for the Harrison Senior facility row (or confirmation that it doesn't exist). |

---

## V6.1.5 sprint (3 Jun 2026) — Pricing Engine → Perplexity Sonar

Tag: **`v6.1.5-pricing.0`** (planned) · workstream: `atlas/docs/V6_1_5_TRACKER.md` · plan: `atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V6_1_5_PRICING.md`.

> **Decision IDs rebased per §0a (DR-V615):** the plan assigned `D-057 → D-064`, but V6.2 already consumed `D-057 → D-065`. V6.1.5 therefore uses **`D-066 → D-073`**; migrations rebase `0034/0035 → 0036/0037`. These rows are **placeholders seeded at T-PRC-0** (status ◐) — rationale fleshed out at T-PRC-1, finalised at T-PRC-6.

| ID    | Topic                                                                           | Status                              | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | ------------------------------------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-066 | Move pricing engine to Perplexity Sonar end-to-end                              | ✅ Shipped (T-PRC-0 seed)           | Viktor, 3 Jun 2026. Closes the 4 research-layer gaps: no `max_uses` search cap, first-class `citations[]`, JSON-schema responses, `sonar-reasoning-pro` thesis test. Flat cost vs Anthropic. Perplexity is the ONLY provider in `lib/pricing/*`; fail-loud on Sonar errors, no Anthropic fallback.                                                                                                                                                                                                                                                                                                                                                                                                         |
| D-067 | SDK vs direct fetch — chose **direct fetch** (no SDK)                           | ✅ Resolved (T-PRC-1)               | Claude Code, 3 Jun 2026. Every existing LLM call in the repo uses raw `fetch` (comp-researcher, csv-column-mapper, ask-juno) and CF Pages is edge-only (SDKs often assume Node). Perplexity is OpenAI-compatible, so `lib/llm/perplexity-client.ts` calls `fetch` directly — **zero new deps** (sidesteps Hard Rule #3 entirely; no SDK could be live-verified without the key anyway). Endpoint overridable via `PERPLEXITY_API_URL` (default `https://api.perplexity.ai/chat/completions`) so the live smoke can correct the path with no code change. **Validated live 3 Jun: 200 OK on `/chat/completions` (no `/v1`), `response_format` json_schema + top-level citations both work on `sonar-pro`.** |
| D-068 | Models: `sonar-pro` standard + `sonar-reasoning-pro` for buyer-migration thesis | ✅ Shipped                          | Viktor, 3 Jun 2026. One model per call, predictable cost; `sonar-reasoning-pro` reserved for the one CoT-benefiting call (T-PRC-5 thesis).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| D-069 | Citations as JSONB on `pricing_briefs.citations` + `comps.source_url`           | ✅ Shipped (T-PRC-1/2/3)            | Viktor, 3 Jun 2026. **DR-A:** columns land on the canonical `pricing_briefs` table (not legacy `pricing_runs`); `pricing_llm_calls.run_id` is a correlation id (not an FK — V6.1.5-004). `comps.source_url` pre-existed; mig 0037 added `relist_count`/`first_listed_at`/`current_dom_days`. The Sonar comp-research path carries `citations[]` on its output; persisting them to `pricing_briefs.citations` + per-$/sqft source chips lands with the brief swap (T-PRC-3, V6.1.5-006).                                                                                                                                                                                                                    |
| D-070 | Framework prompts in `lib/pricing/prompts/`, not code string literals           | ✅ Resolved (T-PRC-2)               | Viktor, 3 Jun 2026. Stored as edge-safe `.ts` template modules (`system-base.ts`, `comp-research-user.ts`) with `{{var}}` placeholders, NOT `.md`/`${var}` — CF Pages edge has no `fs` to read a file at request time (**deviation V6.1.5-002**). Intent preserved: dedicated, versioned, hash-logged files separate from call-site logic; `prompt_hash` (salted SHA-256 of system + template) logged per call. Framework §3.1–3.3 reproduced verbatim.                                                                                                                                                                                                                                                    |
| D-071 | Buyer-migration thesis as a separate Sonar call (not folded into the brief)     | ✅ Shipped                          | Viktor, 3 Jun 2026. One model per call; thesis output is a first-class field on the brief, not buried in prose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| D-072 | Stuck-listing tracker — `relist_count` + `current_dom_days` + `first_listed_at` | ✅ Shipped                          | Viktor, 3 Jun 2026. Surfaces market dynamics the LLM summary misses (in-sub-cut actives with DOM > 180 or relist ≥ 2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| D-073 | Fail-loud on Sonar errors — no Anthropic fallback in pricing (+ feature-flag)   | ✅ Shipped (T-PRC-1; flipped 3 Jun) | Viktor, 3 Jun 2026. The adapter throws a typed `PerplexityError` on 4xx/5xx/timeout/bad-shape — **no fallback, no retry to a second provider/model** (unit-tested: exactly one fetch, never hits anthropic). Missing key = hard error, not a silent skip. **Feature-flag sub-decision: option (b)** — `PRICING_LLM_PROVIDER` (default `anthropic`) keeps the live pricing path on Anthropic until the Sonar path is verified, then an atomic flip; the shipped flag-ON state has no Anthropic in the pricing path. The StatusDot surfacing of `pricing_llm_calls` failures lands with the UI in T-PRC-2/3.                                                                                                 |

## Ask Juno v2 (6 Jun 2026) — in-app agentic assistant

Plan: the "Ask Juno v2" build prompt · audit: `atlas/docs/agent/AUDIT.md` · Phase 1 design: `atlas/docs/agent/PHASE_1_DESIGN.md`. Phases 1–2 are the read-only MVP; nothing mutates without human approval; calc engine frozen; no new deps.

| ID    | Topic                                                                  | Status                    | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ---------------------------------------------------------------------- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-074 | Agent model — config-driven `AGENT_MODEL`, default `claude-sonnet-4-6` | ✅ Locked (Viktor, 6 Jun) | CF secret, trimmed (V6.1.5-013 lesson). NOT v1's hardcoded `max_tokens: 1024` — `maxTokensFor(stepType)` (small for plan/tool-routing, large for synthesis). Build the two-tier seam now (`modelForStep(type)`) so Phase 2 can escalate the single final cross-engine synthesis step to a stronger model with no rework; everything defaults to the Sonnet config for now.                                                              |
| D-075 | Run ceilings — soft pause vs hard abort + pre-call budget gate         | ✅ Locked (Viktor, 6 Jun) | Per-run defaults 20 steps / $0.50; hard caps 40 / $2.00. The SOFT ceiling PAUSES and asks "continue?" (does not abort); the HARD cap stops. Budget enforced server-side BEFORE each call on a max-output estimate, trued up after. Authoritative spend = `SUM(agent_llm_calls.cost_usd)` (crash-proof ledger; `agent_runs.cost_spent_usd` is a cache).                                                                                  |
| D-076 | Approval matrix (record now; built in Phase 4)                         | ✅ Locked (Viktor, 6 Jun) | Operational proposals (actuals, risk) = editor+. Financial/structural — anything that version-bumps or triggers re-approval (land cost, build $/SF, LTC, rates, project create/update) = **super_admin only**. Preserve v1's trivial auto-execute floor (editor+, ≤ $10k, unsnapshotted, batch < 5, actuals/risk only) — v2 must not be more restrictive than v1 for the trivially-safe case. Above the floor routes propose → approve. |
| D-077 | Schema — extend, don't duplicate                                       | ✅ Locked (Viktor, 6 Jun) | `agent_actions` (Phase 4) FKs into the existing `atlas.suggestions` — one approval queue. New `agent_llm_calls` table (don't loosen `pricing_llm_calls` CHECKs). When approve-executes is built (Phase 4) it must be GENERIC through the existing services, closing the dormant `applied` state for ALL suggestions, not just agent ones.                                                                                               |
| D-078 | Agent role gates                                                       | ✅ Locked (Viktor, 6 Jun) | Run an agent = editor+. `viewer` = read-only of completed runs. `viewer_basic` = nothing agent-facing. RLS is the backstop (authenticated read); the route layer is the primary gate.                                                                                                                                                                                                                                                   |
