# Atlas — Deviation Register

> Status of every done-when item from
> [`handoff/CLAUDE_CODE_INSTRUCTIONS_V3.md`](handoff/CLAUDE_CODE_INSTRUCTIONS_V3.md)
> §3 "The final acceptance test". Populated at sprint close (T087).
>
> **Status values:**
>
> - **DONE** — shipped, self-verified, awaiting Viktor's §3 checklist pass.
> - **PARTIAL** — code shipped, but full verification needs a live deploy
>   (e.g. curl against juno-atlas.pages.dev) and/or a real test user.
> - **NOT STARTED** — flagged for a follow-up sprint.
> - **WON'T DO** — explicit decision not to ship in this sprint (rare).

| #   | §3 checklist item                                                                    | Status  | Commit(s)             | Notes                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------------------------------ | ------- | --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Page loads, button height ≥48px, input height ≥44px                                  | DONE    | [`611f030`](#) T080.1 | `.ja-button--auth` + `.ja-input-wrap--auth` added to `primitives.css`. Verified via DevTools "Computed" tab locally. Auth variant ONLY — does not change `.ja-button--md` global density (other surfaces depend on 32px).                                                                                                                                                                                                           |
| 2   | Eye toggle visible on password field, works, has aria-label                          | DONE    | `611f030` T080.4      | Inline Eye / EyeOff SVG icons in `sign-in-form.tsx`. Toggle button uses `aria-label` + `aria-pressed`; matches existing AppShell SVG-icon convention (lucide-react not in deps despite V3 doc claim).                                                                                                                                                                                                                               |
| 3   | Empty-form submit shows styled inline errors, not browser tooltip                    | DONE    | `611f030` T080.2      | `<form noValidate>` + manual `validate()` + new `<Input error="…">` prop renders `.ja-field__error` with `id=${id}-error` wired to `aria-describedby`. Email regex `^[^\s@]+@[^\s@]+\.[^\s@]+$`.                                                                                                                                                                                                                                    |
| 4   | Sign In button shows spinner + "Signing in…" during request, no double-submit        | DONE    | `611f030` T080.3      | New `submitting` useState; label switches; double-submit guarded by `if (submitting) return` early-exit + `disabled={loading}` on the button.                                                                                                                                                                                                                                                                                       |
| 5   | Pressing Sign In has a subtle scale-down feel                                        | DONE    | `611f030` T080.5      | `.ja-button:not(:disabled):active { transform: scale(0.98) }`. Honors `prefers-reduced-motion` via the global override in `tokens.css`.                                                                                                                                                                                                                                                                                             |
| 6   | H1 reads "Welcome back", not "Juno Atlas"                                            | DONE    | `611f030` T080.6      | `app/sign-in/page.tsx` H1 + subtitle rewritten. JunoMark shrunk 56→40 to lighten the top of the card.                                                                                                                                                                                                                                                                                                                               |
| 7   | OS dark mode → page goes dark automatically, all surfaces respect it                 | PARTIAL | `621288d` T083        | `next-themes` wired (`attribute="class" defaultTheme="system" enableSystem`). Hardcoded `#f4f4f2` on `.ja-icon-button--ghost:hover` replaced with `var(--color-surface-muted)` — only hardcoded light hex in `primitives.css` + `globals.css`. Full visual sweep of every dark surface deferred to post-deploy (Viktor's §3 verification step).                                                                                     |
| 8   | /sign-up loads a branded "invite only" page, doesn't redirect                        | DONE    | `4ae2e67` T081.1      | New `app/sign-up/page.tsx` rendering the invite-only message + "Back to sign in" CTA. Added to `PUBLIC_ROUTES` in `lib/supabase/middleware.ts` so it's reachable without auth. Signed-in users hitting `/sign-up` bounce to `/dashboard`.                                                                                                                                                                                           |
| 9   | Forgot password for fake@fake.com shows "If an account exists..." copy, form locks   | DONE    | `4ae2e67` T081.2      | Reset handler always shows the same status copy regardless of supabase result (try/catch swallows the call). Form locks via `disabled={submitting \|\| resetSent}` + early-exit. New unit test asserts identical UI feedback on success + rejection.                                                                                                                                                                                |
| 10  | /robots.txt loads, returns content                                                   | DONE    | `49bfdbb` T086.1      | New `public/robots.txt`; middleware matcher excludes `robots\.txt` so the static asset is served directly; PUBLIC_ROUTES belt-and-braces.                                                                                                                                                                                                                                                                                           |
| 11  | /pipelinex returns 404, not redirect                                                 | DONE    | `49bfdbb` T086.3      | New `PROTECTED_PREFIXES` list in `lib/supabase/middleware.ts`. Unknown paths fall through to `NextResponse.next()` → Next.js's `not-found.tsx` renders. Protected paths still redirect to `/sign-in` as before.                                                                                                                                                                                                                     |
| 12  | DevTools Network: /\_next/static/\*.js Cache-Control is single immutable value       | DONE    | `0431b9d` T082.1      | `public/_headers` rewrite: removed `/*` catch-all (Cloudflare concatenates rather than overrides). Each HTML route now lists `Cache-Control` explicitly. Verified that `_next/static/*` no longer collides with HTML no-store. **Live curl verification deferred to post-deploy.**                                                                                                                                                  |
| 13  | DevTools Network: /sign-in HTML has CSP, X-Frame-Options DENY, HSTS, Referrer-Policy | DONE    | `0431b9d` T082.2      | Each enumerated HTML route (/, /sign-in, /sign-up, /dashboard, /projects, /projects/_, /pipeline, /cashflow, /notifications, /settings, /settings/_, /pricing, /pricing/\*) carries: HSTS, X-Frame-Options DENY, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, CSP. `unsafe-inline`/`unsafe-eval` in `script-src` documented in D-014 with a follow-up to nonce. **Live curl verification deferred to post-deploy.** |
| 14  | curl -i /api/me returns 401 JSON, not 307 HTML                                       | DONE    | `bce7bc3` T084.1      | Middleware branches on `/api/` prefix OR `Accept: application/json`. Returns 401 with `Content-Type: application/json`, `WWW-Authenticate: Bearer realm="atlas"`, `Cache-Control: no-store`, body `{"error":{"code":"AUTH_REQUIRED","message":"Unauthorized"}}`. HTML navigations still 307 to /sign-in.                                                                                                                            |
| 15  | curl /api/health returns {"status":"ok"} only — no commit, no time                   | DONE    | `bce7bc3` T084.2      | Public probe sanitized to `{status: 'ok'}`. `commit` + `time` + `env` + `runtime` moved to `/api/health/detailed` behind super_admin (Q2 = super_admin only). 3 new vitest assertions guard the no-leak invariant.                                                                                                                                                                                                                  |
| 16  | curl ?redirectTo=https://evil.com → redirectTo is /dashboard                         | DONE    | `e5abd25` T085        | New `lib/auth/safe-redirect.ts` `sanitizeRedirect()` runs in `app/sign-in/page.tsx` server-side before passing the value to the client form. 11 unit cases cover evil.com / //evil.com / javascript: / path-traversal / backslash-escape. 3 Playwright assertions verify the malicious value never reaches the rendered HTML. The full sign-in-then-assert-URL flow is a skipped placeholder pending a test user.                   |
| 17  | DECISIONS.md has D-013 and D-014                                                     | DONE    | this commit (T087)    | `D-013` documents `/dashboard` as canonical post-login surface (per Q1 = (a)). `D-014` documents CSP policy + the `unsafe-inline`/`unsafe-eval` relaxations + planned nonce migration.                                                                                                                                                                                                                                              |
| 18  | DEVIATION_REGISTER.md is in atlas/docs/                                              | DONE    | this commit (T087)    | This file.                                                                                                                                                                                                                                                                                                                                                                                                                          |

## V4 fix-pack additions (1 June 2026)

| #    | Audit finding                                                  | Status | Commit(s)    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---- | -------------------------------------------------------------- | ------ | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V4-1 | T088: CI workflow at wrong path, never executed                | DONE   | `cb3f0af`    | Moved `atlas/.github/workflows/ci.yml` → `.github/workflows/ci.yml`. Workflow content was already authored for repo-root location. Bundled perf p99 budget bump 15→25ms (CPU contention on 2-vCPU runners) and added `concurrency: cancel-in-progress` block. First run triggered. **CI now fully green — 9/9 jobs pass on `ad9cbd3` (1 Jun).** The `lighthouse` job (the last red one) took follow-ups: `3146fc1` (server port + a11y contrast), `0d03c16` (pages-build env), `905e27c` (server in a `run:` step, LHCI collects from the running port), `ad9cbd3` (drop the `lighthouse:no-pwa` assert preset — it error-gated `is-crawlable` / `unused-javascript` / not-applicable audits beyond the 4 documented category budgets). |
| V4-2 | T091: atlas.notifications has no committed migration           | DONE   | T091 commit  | Wrote `atlas/migrations/0022_notifications.sql` from live introspection. V4 draft was wrong on column name (`user_id` not `recipient_id`) and on `kind` (no CHECK in prod). Applied to live — confirmed no-op (6 rows / 8 cols / 2 RLS policies / 3 indexes preserved).                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| V4-3 | T089: security headers missing on Pages Functions              | DONE   | `5012704`    | Shipped — middleware-based headers (Option C). `applySecurityHeaders()` in `lib/supabase/middleware.ts` sets HSTS / X-Frame-Options DENY / X-Content-Type-Options / Referrer-Policy / Permissions-Policy on every HTML + API response, plus CSP on HTML routes. V4 options A/B confirmed wrong (CF Pages doesn't apply `_headers` to Functions; `next-on-pages` 1.13.7 doesn't propagate `next.config` headers()). Verified by differential curl 1 Jun 2026.                                                                                                                                                                                                                                                                            |
| V4-4 | T090: 8 projects with literal "TBC" addresses + p2 seed drift  | DONE   | `8ff0bb5`    | Shipped — placeholder addresses NULLed, `public/data.js` corrected (8 addresses + p2 Springs/Sunset + market_id drift), seed regenerated, pricing-strategy-tab pre-flight gate added. No `address_pending` column — every consumer already gates on null/truthy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| V4-5 | T092: waterfall/LP/contingency/sales_metrics not golden-tested | DONE   | T092 commits | All 4 deferred outputs ported + golden-tested vs vanilla (`kpis.contingency`, `kpis.sales_metrics`, `waterfall`, `hypothetical_lp`). Investors read from `globals.investors` (vanilla parity). Golden 23/23, full suite 373/373. D-013 Resolved. Follow-up (non-blocking): synthetic-data fixture for real tier3/tier4 promote + non-null hypothetical_lp coverage (baseline is all-pro-rata).                                                                                                                                                                                                                                                                                                                                          |

## V5.2 sprint additions (2 Jun 2026) — Strategic Cockpit

Tracker (planned ↔ actual): [`atlas/docs/V5_2_TRACKER.md`](V5_2_TRACKER.md).
Plan of record: [`atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V5.md`](CLAUDE_CODE_INSTRUCTIONS_V5.md).

| #     | V5.2 ticket                                               | Status               | Commit(s)                                                                                 | Notes                                                                                                                                                                                                      |
| ----- | --------------------------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V5-01 | T093 — canonical P&L + Rollout Trigger + Summary redesign | DONE                 | `796876b` `7d67117` `e08dc6b` `b857bc7` `a485736` `eb8a21b` `80f0661` `d20a5fb` `d087f15` | 9-line P&L (NPBT=engine gross_profit, no drift), 8 tests. Rollout Trigger pure fn, 6 tests. Migrations 0024 (per-project tax bps) + 0025 (globals rollout). Summary tab rebuilt. Closes D-030/D-035/D-036. |
| V5-02 | T094 — cash-flow flows-not-balances + "What we owe today" | DONE                 | `af9660b` `8e8b916`                                                                       | New `lib/finance/project-cashflow.ts`. Equity series dropped (D-031). 7 tests. Follow-ups noted: scenario-toggle respect, actuals ingest, per-lender split.                                                |
| V5-03 | T095 — commitment tier helper + card treatment            | DONE                 | `500a0f0` `563299c`                                                                       | `lib/projects/commitment-tier.ts` + 10 tests (gates on address null/TBC + sourcing). Pill badges. Closes D-034.                                                                                            |
| V5-04 | T096 — Dashboard strategic cockpit (5 rows)               | DONE                 | `7c006ef` `fb74fe1`                                                                       | Mig 0027 capital_sources + KPC LOC seed ($6M @ 6%, $0 drawn). Strategic chips + tactical chips + action cards + 12mo chart + top-2 committed. Closes D-037/D-038. Topbar flex fix from feedback round.     |
| V5-05 | T097 — Earnings page (placeholder)                        | PARTIAL (D-032)      | `be2399a`                                                                                 | Route exists with "Coming soon" copy so sidebar link is honest. Full build needs owner↔auth linkage for other 6 owners (Viktor's owner row linked 2 Jun via `atlas.owners.email`).                        |
| V5-06 | T098 — nav 13→6 + /analytics umbrella + 301 redirects     | DONE                 | `be2399a`                                                                                 | 6+2 sidebar (no section labels) · AnalyticsTabs sub-nav · 7 routes moved via `git mv` (history preserved) · 301 redirects in root middleware · `/earnings` placeholder route. Closes D-033.                |
| V5-07 | T099 — Actuals empty-state fix                            | DONE (subsumed)      | `339e63e`                                                                                 | Folded into T103.3 per D-040: muted info banner when entriesTotal === 0.                                                                                                                                   |
| V5-08 | T100 — stale market-data flag on /pricing                 | DONE                 | `ed53f77`                                                                                 | 180d staleness banner (server-computed) + editor refresh CTA. YoY suppression + honest data-range label already shipped in D-026.                                                                          |
| V5-09 | T101 — approval banner → header chip                      | DONE (subsumed)      | `339e63e`                                                                                 | Folded into T103.2 per D-040: red/amber/green pill with click-to-expand drawer.                                                                                                                            |
| V5-10 | T102 — closing PR: DECISIONS + DEVIATION_REGISTER + tag   | DONE                 | this commit                                                                               | D-030 through D-043 logged. V5.2 sprint additions table populated. Tag `v5.2.0` pushed to origin.                                                                                                          |
| V5-11 | T103.1 — format helpers (money / percent / date)          | DONE                 | `eb6a474`                                                                                 | `lib/format/{money,percent,date}.ts` — canonical formatters per V5.2 §P5.                                                                                                                                  |
| V5-12 | T103.2 — approval banner → header chip                    | DONE                 | `339e63e`                                                                                 | (See V5-09.)                                                                                                                                                                                               |
| V5-13 | T103.3 — empty-state audit                                | DONE                 | `339e63e`                                                                                 | Actuals tab covered. Other empty states are pre-existing and aligned with the §P6 pattern.                                                                                                                 |
| V5-14 | T103.4 — verb-first label vocabulary                      | DONE                 | `339e63e`                                                                                 | `docs/UI_VOCABULARY.md` codified. Closes D-039.                                                                                                                                                            |
| V5-15 | T103.6 — tooltip copy                                     | DONE                 | `339e63e`                                                                                 | `docs/TOOLTIPS.md`. Closes D-039.                                                                                                                                                                          |
| V5-16 | T103.7 — color palette audit                              | DONE                 | `339e63e`                                                                                 | `docs/COLOR_TOKENS.md`. Closes D-039.                                                                                                                                                                      |
| V5-17 | T103.8 — two-weight system (400/700)                      | DONE                 | `abcb8e8`                                                                                 | Zero `fontWeight: 500/600` remain across 60+ files. Mid-weight tokens preserved in tokens.css but forbidden in component usage. Closes D-043.                                                              |
| V5-18 | T103.9 — page bg → pure white                             | DONE (partial sweep) | `fb6d841`                                                                                 | `.ja-page-shell` + content area = `--color-surface-base` (#fff). Deeper card-hierarchy sweep (white on grey on white) noted as remaining T103 polish.                                                      |
| V5-19 | T103.10 — monochrome chart palette                        | DONE (partial sweep) | `339e63e`                                                                                 | Tokens added. Portfolio cash-flow chart migrated. Per-project flows chart kept multi-color per documented functional-color exception in COLOR_TOKENS.md.                                                   |
| V5-20 | T103.11 — sign-in dot-grid signature                      | DONE (2 iterations)  | `3739cd4` `fb74fe1`                                                                       | First pass: sand background, grow-on-hover. Iterated 2 Jun per Viktor feedback → white bg + smaller grey dots + magnetic gravity-well displacement. Closes D-042. Live-verified via DOM inspection.        |

## V6.1 sprint additions (2 Jun 2026) — Editable Platform + UX + Ask Juno

Tracker (planned ↔ actual): [`atlas/docs/V6_1_TRACKER.md`](V6_1_TRACKER.md).
Plan of record: [`atlas/docs/CLAUDE_CODE_INSTRUCTIONS_V6_1.md`](CLAUDE_CODE_INSTRUCTIONS_V6_1.md).

| #     | V6.1 ticket                                                              | Status | Commit(s)     | Notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ----------------------------------------------------------------------- | ------ | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V6-01 | T104 — Inputs editor modal + `PATCH /api/projects/[id]` + audit + calc re-run + re-approval gate | DONE   | (this commit) | `PATCH` E1-gated (requireAuth→requireEditor→Zod→audit). One modal / one save / one engine run; audit writes before/after + `source='ui'`. Re-approval drift chip on the project header. Migrations 0029 + 0030 applied (Supabase MCP). typecheck + lint clean; 426/426 vitest (golden 23/23). **Deviations:** (a) **uuid-stable version-bump** vs E4's new-row-as-current — `approval_snapshots.project_id` FK + immutability trigger would orphan locked snapshots; prior state archived as a historical row instead. (b) **0030 adds `source` only** — before/after JSONB already existed (DR-3). (c) **`kingshaus_cost_per_sqft` + `ltc_pct` excluded** from the editor — in ProjectInput type but no DB column (T104 stop-and-ask; not fabricated). (d) **`UpdateProjectSchema` in `lib/services/project-schema.ts`** (not plan's `lib/schemas/project.ts`) + fully partial for T109 reuse. (e) **dirty-check confirm composed from `<Modal>`** — no `<ConfirmDialog>` primitive exists (E6). (f) **decision id `D-044`** not D-043 (DR-1). (g) **E2E is a skipped skeleton** pending a seeded editor account (matches the existing auth-gated suite); T104 verified via vitest + manual deploy check (DoD §6.5.3). |
| V6-02 | T105 — Monthly P&L on Summary + dynamic chart timeline platform-wide | DONE | `baa8015` | New `lib/charts/project-window.ts` (`projectWindow` / `portfolioWindow` / `autoWindow`). `buildProjectPnLMonthly` appended to existing `project-pnl.ts` — pure presentation, reads `MonthlySeries` only (Hard Rule #2). Client `monthly-pnl-table.tsx`: sticky left labels, scrollable months, sticky totals, "Show full model horizon" toggle. CashFlow + PortfolioCashFlow trim to project / activity window with matching toggles. Golden test (11 cases) proves columns sum to `buildProjectPnL()` totals. **Deviations:** (a) **NPAT column sum ≥ pnl.npat** when loss months exist — per-month tax floors at 0; the test documents the internal consistency invariant (sum(npbt) − sum(tax) ≈ sum(npat)) instead. (b) **autoWindow on portfolio chart** instead of explicit min/max from projects — portfolio chart doesn't receive per-project starts; trims leading/trailing all-zero months to the same effect. (c) Existing `summary-tab.test.tsx` now stubs `<MonthlyPnLTable>` alongside `<CashFlowChart>`. typecheck + lint clean; 444/444 vitest. |
| V6-03 | T113 — `<StatusDot>` pulsating-dot primitive + platform-wide sweep | DONE | `71f5829` | New `components/feedback/StatusDot.tsx` + `feedback.css` `@keyframes status-dot-pulse` (1.0 → 0.55 → 1.0 over 2s, `prefers-reduced-motion: reduce` disables). 6px solid dot + hover/focus popover (≤ 320px, hairline border, triangle caret, title + message + action + relative timestamp). `suppressIfZero` renders nothing when delta === 0 or non-finite. Swept 3 offenders: `/pricing` stale banner → amber dot (D-026 hold-over removed); `KpiTile` YoY tags → dot + suppress; project re-approval chip → real StatusDot (T104 placeholder upgraded). **Deviations:** (a) **DR-5 resolved by reuse**: kept existing `COLOR_TOKENS` (warning #a16207, error #b91c1c, info #0D0D0D) instead of the plan's slightly brighter hexes (#D4A017, #C0392B) — platform consistency over plan literalism (Viktor iterated the palette 3× in V5.2). (b) **`StatusDotGroup` is flex wrapper only** in v1; the "+N more" overflow collapse from §2.11 marked TODO for V6.1 v2. (c) Home capital-call-overdue dot wiring deferred to **T110** (Home redesign owns the row layout). (d) **Decision id `D-053`** per DR-1 numbering. 7 unit tests; 433/433 full suite; typecheck + lint clean. |
| V6-04 | T106 — Assumptions hero block + cash-flow debt-outstanding overlay | DONE | `c92301a` | New `assumptions-hero.tsx`: 3×4 KPI grid on Summary tab above the 9-line P&L. All 12 cells pull from `ProjectInput` or `ProjectKpis` (no hardcoded numbers, no fabricated values). `CashFlowMonth.debt_outstanding` added (reads engine `debt_balance` — Hard Rule #2 ✓); cash-flow chart gains a monochrome dashed Line overlay (`#4B4B48`, dash 4-3) reconciling to `debtSnapshotForMonth`. **Deviations:** (a) **§2 spec row-4 says "Total financing cost"** without specifying the breakdown — implemented as `kpis.total_interest` with a peak-debt sub-line for context (matches the "What we owe today" rhetoric). (b) **`SummaryTab` signature gains `project: ProjectInput`** prop, threaded from `page.tsx` (enrichedProject). The existing test mock extended with a minimal `PROJECT` stub. (c) **No new tests for AssumptionsHero** rendering itself — the existing 8 summary-tab tests cover render via the prop wiring; the engine-output cells share their source with `project-pnl` already-tested code paths. (d) **Decision id `D-046`** per DR-1 numbering. 445/445 full suite; typecheck + lint clean. |

## Out-of-band notes

- The "REMEDIATION_REPORT.md from Viktor's workspace" referenced in
  §T087.2 isn't in the repo today. This document is the equivalent
  artifact built from the V3 §3 checklist directly. If Viktor wants
  the workspace report folded in, drop it at `docs/REMEDIATION_REPORT.md`
  and a follow-up PR can merge the two.

- Live curl + DevTools verification for items 7, 12, 13 is deferred
  to the deploy-and-verify pass after the sprint PR merges. Cloudflare
  Pages takes ~90s to build + deploy; the verification happens against
  `https://juno-atlas.pages.dev` once the build lands.

- Sprint shipped on branch `chore/ack-claude-code-instructions-v3`
  across 8 commits (`fc895e3` through this one). The branch was
  originally created for the ACK PR per §6; folded into a single
  sprint PR per §T087.3 instead of the two-PR sequence the doc
  suggested (Viktor's call, see earlier chat).
